# Program Progress Matrix

This document is the durable human-readable rollup for progress toward a WordPress distribution whose first-party runtime logic is authored in Haxe and whose generated artifacts remain functional for the WordPress ecosystem.

Beads remains the task database. This matrix is a status ledger over Beads issues, receipts, manifests, and PRD milestones. Update it in the same change whenever milestone status, scope, evidence gates, Gutenberg split policy, or completion estimates change.

Last updated: 2026-07-02
Source checkpoint: `WPHX-312 bounded domain closure`
Tracking issue: `WPHX-000.02`

## Super Progress

Current super progress toward "WordPress completely in Haxe and functional": **46.4%**.

This is an evidence-weighted program estimate, not source-line completion. It gives credit for proven target foundations, ABI/linker infrastructure, vertical WordPress slices, and executable parity gates, but it discounts shell/scaffold work and gives almost no credit for Gutenberg/browser ownership until `WPHX-400` and `WPHX-500` start producing package-level receipts.

| Area | Weight | Current | Contribution | Basis |
| --- | ---: | ---: | ---: | --- |
| Program governance, baselines, manifests, Beads, receipts | 10 | 95 | 9.5 | `WPHX-000`, `WPHX-800`, baseline receipts, locked sibling repo policy |
| PHP target feasibility, ABI, linker, public boundary foundation | 20 | 100 | 20.0 | `WPHX-100` and `WPHX-200` closed |
| WordPress PHP runtime Haxe ownership | 40 | 38.4 | 15.4 | `WPHX-300` has verified vertical slices, first-party PHP manifest ownership/split closure, bounded WPHX-306 users/auth, WPHX-307 posts/query, WPHX-308 taxonomy/comment, WPHX-309 routing/template, WPHX-310 theme/theme JSON/template hierarchy, and WPHX-313 media/filesystem/upload domain closures, plus WPHX-314 blocks/interactivity PHP surface inventory, first typed adapter-contract candidate, block parser/render copied-oracle fixture, block supports/bindings copied-oracle fixture, block patterns registry copied-oracle fixture, block hooks insertion copied-oracle fixture, style engine copied-oracle fixture, HTML API tag processor copied-oracle fixture, interactivity API copied-oracle fixture, core block renderer copied-oracle fixture, selected upstream PHPUnit ratchet scope, and installed-style block rendering gate, plus WPHX-312 HTTP/cron/mail/feed/embed surface inventory, first typed adapter-contract candidate, side-effect-safe HTTP/cron/mail oracle-source fixture, side-effect-safe feed/embed/HTTPS oracle-source fixture, AI HTTP fake-transport oracle-source fixture, trackback endpoint oracle-source fixture, privacy request mail oracle-source fixture, remote feed/oEmbed fake-HTTP oracle-source fixture, selected upstream PHPUnit ratchet scope, privacy request admin-state oracle-source fixture, cron dispatch/lock oracle-source fixture, PHPMailer setup oracle-source fixture, OPML links output oracle-source fixture, RSS2 feed template oracle-source fixture, Atom feed template oracle-source fixture, RSS2 comments feed template oracle-source fixture, Atom comments feed template oracle-source fixture, RDF feed template oracle-source fixture, RSS 0.92 feed template oracle-source fixture, deprecated embed/RSS wrapper oracle-source fixture, deprecated oEmbed wrapper oracle-source fixture, oEmbed provider registry oracle-source fixture, WP_Embed cache/autoembed oracle-source fixture, oEmbed REST controller oracle-source fixture, MagpieRSS parser oracle-source fixture, MagpieRSS fetch/cache oracle-source fixture, MagpieRSS display-helper oracle-source fixture, RSS widget helper oracle-source fixture, WP_Widget_RSS class oracle-source fixture, RSS block renderer oracle-source fixture, HTTP proxy oracle-source fixture, HTTP Requests bridge oracle-source fixture, HTTP IXR client oracle-source fixture, feed cache transient oracle-source fixture, SimplePie file HTTP oracle-source fixture, HTTP response object oracle-source fixture, HTTP cookie object oracle-source fixture, HTTP encoding oracle-source fixture, WP_Http helper oracle-source fixture, WP_Http parser/header oracle-source fixture, HTTP API wrapper/safety oracle-source fixture, HTTP parse-url/class-http shim oracle-source fixture, HTTP transport callback/test oracle-source fixture, WP_Http request orchestration oracle-source fixture, WP_Http block-request policy oracle-source fixture, WP_Http request proxy/safety option oracle-source fixture, WPHX PHP recorded HTTP transport parity gate, and controlled cron/mail transport installed-style gate, plus first WP_Http_Encoding, WP_HTTP_Proxy, WP_HTTP_Response, WP_Http_Cookie, WP_Http block-request, WP_Http IP address, WP_Http browser redirect compatibility, WP_Http redirect validation, WP_Http absolute URL, WP_Http redirect orchestration, WP_Http processResponse, WP_Http chunkTransferDecode, WP_Http processHeaders line-decision, WP_Http buildCookieHeader assembly, WP_Http deprecated parse_url wrapper, WP_Http request nonblocking response, WP_Http deprecated transport selection, WP_Http request safety options, WP_Http request method options, WP_Http request redirect options, WP_Http request SSL options, WP_Http request response-size options, WP_Http request stream blocking, WP_Http request stream filename options, WP_Http request invalid URL, WP_Http request blocked-request, WP_Http request preemptive response, WP_Http request header parsing, WP_Http request proxy options, WP_Http request proxy authentication, WP_Http request error response, WP_Http request cookie options, WP_Http request stream destination error, WP_Http request HEAD redirection default, WP_Http request unsafe URL validation, WP_Http request bad-protocol stripping, WP_Http request redirection-copy, WP_Http request null-header normalization, WP_Http request stream default-filename, WP_Http request http_response attachment, and WP_Http request mbstring reset Haxe parity candidates, but much of Core remains unported |
| Installed WordPress distribution behavior and ecosystem gates | 15 | 10.0 | 1.5 | `WPHX-700` has strong early gates, focused WPHX-306 auth, WPHX-307 posts/query, WPHX-308 taxonomy/comment, WPHX-309 routing/template, WPHX-310 theme, WPHX-312 HTTP/cron/mail/feed/embed plus controlled cron/mail transport, WPHX-313 media/upload, and WPHX-314 block rendering installed-style gates, a provisioned selected upstream PHPUnit ratchet where WPHX-312/WPHX-313 groups classify pass/pass and the remaining WPHX-314 baseline failures classify vanilla fail / candidate fail rather than candidate regressions; complete installed-system parity is still not closed |
| Browser, classic JS, Gutenberg package ownership | 10 | 0 | 0.0 | `WPHX-400`, `WPHX-500`, and `WPHX-600` are not yet active implementation tracks |
| Security, performance, reliability, and release closure | 5 | 0 | 0.0 | early security corpus exists, full nonfunctional closure is later-stage work |

The score should move only when evidence moves. A closed task can increase confidence in a slice without implying that the whole source area is ported.

ADR-004 clarifies ownership claims without changing the score: PHP is the privileged compatibility host for current parity, Haxe owns both migrated semantics and adapter intent, and Rust/native/WASM/custom-target providers are future optional providers only for narrowly eligible kernels or explicitly named profiles with PHP fallbacks and native-on/native-off evidence. The 2026-06-29 original-path PHP emission oracle response further clarifies the current compiler split: stock Haxe PHP is the private implementation emitter; WPHX PHP is the staged custom PHP compiler lane currently focused on bounded WordPress original-path public adapters; the portable long-term asset is Haxe-owned semantics plus typed adapter contracts, not generated PHP text. ADR-013 now makes that adapter contract explicit: WPHX PHP lowers typed Haxe source and metadata into Adapter IR file/declaration plans before printing original-path public PHP, and those annotations/IR nodes should be shaped as future inputs to a broader WPHX PHP backend or extracted `reflaxe.php` target without Haxe source rewrites. ADR-015 clarifies the Reflaxe nuance and generated-code quality bar: WPHX PHP uses Reflaxe infrastructure, but it is not yet a mature arbitrary-Haxe `reflaxe.php` target, and promotion to full backend scope requires backend-scale evidence, quality gates, and a later ADR or amendment. ADR-016 changes the focus: WPHX PHP compiler usability is now the near-term priority for new WordPress public PHP emission and compiler-shape gaps, with stock Haxe PHP bounded to private-output/stdphp oracle duty until the `WPHX-COMP-PHP-USABLE` gates move responsibility. ADR-017 keeps stock Haxe PHP as the runtime/std behavior oracle and borrowing source for boot, arrays, maps, iterators, closures, exceptions, strings, JSON, and debug/source-map behavior until a later backend-promotion ADR moves that ownership. A later multi-target track, including a browser/WASM profile potentially based on `back2dos/wasmix`, a Go profile potentially based on `fullofcaffeine/reflaxe.go`, and a Rust profile potentially based on `fullofcaffeine/reflaxe.rust`, is tracked as a long-term research option and does not contribute to current parity progress. Future ownership rollups should distinguish semantic ownership, adapter-contract ownership, emission strategy, execution provider, and evidence level.

2026-07-02 strategy checkpoint: `WPHX-COMP-PHP-ADOPTION-TRACK` accepts ADR-016 and seeds `WPHX-COMP-PHP-USABLE`. The project will focus on making WPHX PHP usable as the primary WordPress PHP compiler path before accumulating more broad Core port debt. The new gates are stock-target/gap inventory, runtime/std strategy, reusable PHP core lowering, a small whole-file WordPress pilot, and adoption CI. The super-progress score stays unchanged because this is strategy and task-shape movement, not new compiler functionality, runtime ownership, or installed WordPress behavior.

2026-07-02 compiler checkpoint: `WPHX-COMP-PHP-GAP-INVENTORY` records the first usable-compiler gate at `manifests/wphx-php/compiler-gap-inventory.v1.json` and `receipts/compiler/wphx-comp-php-gap-inventory.v1.json`. The inventory finds 25 Reflaxe-backed WPHX PHP public-adapter hxmls, 9 stock Haxe PHP private-output hxmls, 46 helper-bridge metadata sites, 18 bootstrap metadata sites, 39 WordPress-profile method adapters, 4 script adapters, 18 unsupported typed-lowering report sites, 6 WPHX PHP runner copy/install surfaces, 11 passing WPHX PHP evidence manifests, and 7 available stock Haxe PHP reference files. It classifies follow-up ownership across runtime/std borrowing, reusable PHP core IR, WordPress profile ABI constraints, temporary fallbacks, and backend pressure while keeping the WordPress profile at zero `PhpRawBlock` occurrences and zero `renderTemplate` calls. The super-progress score stays unchanged because this is gap evidence and routing, not new compiler lowering, runtime ownership, or installed WordPress behavior.

2026-07-02 compiler checkpoint: `WPHX-COMP-PHP-CORE-LOWERING-PILOT` adds the first no-helper/no-profile-adapter typed statement lowering fixture at `fixtures/wphx-php/core-lowering-pilot.hxml`. WPHX PHP now lowers ordinary typed Haxe `if`, `while`, `break`, and `continue` statements for public shell method/global bodies, records `typed.stmt.break`, `typed.stmt.continue`, `typed.stmt.if`, and `typed.stmt.while` in the emission manifest, lints and executes the generated `wp-includes/wphx-core-lowering.php` shell, and adds the case to public-shell snapshots, which now cover 18 generated-shape cases. The refreshed gap inventory now records 26 Reflaxe-backed WPHX PHP public-adapter hxmls and 12 WPHX PHP evidence manifests. The super-progress score stays unchanged because this is compiler capability evidence, not new WordPress runtime ownership or installed WordPress behavior.

2026-07-02 compiler checkpoint: `WPHX-COMP-PHP-RUNTIME-STDLIB-STRATEGY` accepts ADR-017 and records executable stock-Haxe-PHP runtime/std oracle evidence at `manifests/wphx-php/runtime-stdlib-strategy.v1.json` and `receipts/compiler/wphx-comp-php-runtime-stdlib-strategy.v1.json`. The probe compiles release and debug stock-Haxe-PHP variants, executes both with PHP, and observes boot/autoload shape, array/map/iterator behavior, captured closures, exception wrapping/catching, `StringTools`/JSON behavior, source maps, and inline source comments. WPHX PHP now has 3/5 usable-compiler child gates evidenced, and the refreshed gap inventory records 13 WPHX PHP evidence manifests. The super-progress score stays unchanged because this is runtime/std borrowing strategy evidence, not WPHX runtime ownership, new WordPress runtime ownership, or installed WordPress behavior.

2026-07-02 compiler checkpoint: `WPHX-COMP-PHP-WHOLE-FILE-PILOT` adds the first minimized whole-file ownership gate for a real WordPress source file. WPHX PHP now emits the complete original-path `wp-includes/class-http.php` file without stock Haxe PHP public shape fallback, Haxe bootstrap, or helper bridge, records `adoption_mode="whole_file_owned"` in `segment_plans`, and compares oracle/candidate behavior for `_deprecated_file` payload, `basename(__FILE__)`, `ABSPATH`/`WPINC` path construction, and `require_once` idempotence. The public-shell snapshot lane now covers 19 generated-shape cases, and the refreshed gap inventory records 27 Reflaxe-backed WPHX PHP public-adapter hxmls, 5 script adapters, and 14 WPHX PHP evidence manifests. WPHX PHP now has 4/5 usable-compiler child gates evidenced. The super-progress score stays unchanged because this is a minimized compiler ownership gate, not installed WordPress behavior, `WP_Http` ownership, live transport behavior, or broad Core runtime ownership.

2026-07-02 compiler checkpoint: `WPHX-COMP-PHP-ADOPTION-CI` closes the bounded usable-compiler gate at `manifests/ci/wphx-php-adoption-ci.v1.json` and `receipts/compiler/wphx-comp-php-adoption-ci.v1.json`. The executable gate now runs eleven required checks across gap inventory, raw-block/template policy, public-shell snapshots, runtime/std strategy, core-lowering, whole-file ownership, bootstrap autoload, bootstrap error handling, bootstrap debug evidence, and the selected embed/HTTPS module-function adapter gates. It verifies generated PHP quality, `unsupported=[]` for claimed boundaries, zero inline public raw PHP bodies, zero active adapter templates, public-shell snapshot coverage, runtime/std/bootstrap/debug probes, and bounded stock Haxe PHP fallback surfaces. WPHX PHP now has 5/5 usable-compiler child gates evidenced and bounded parallel Core work may resume when new public PHP emission or compiler-shape pressure routes through WPHX PHP and keeps the adoption gate green. The super-progress score stays unchanged because this is a compiler-readiness gate, not installed WordPress behavior, full arbitrary-Haxe backend maturity, WPHX runtime/std ownership, live HTTP behavior, Gutenberg/browser ownership, or abandonment of stock Haxe PHP as private implementation/stdphp oracle.

2026-07-02 compiler checkpoint: `WPHX-COMP-PHP-MODULE-FUNCTION-ADAPTERS` adds the first selected WPHX-312.04 feed module-function original-path adapter gate at `manifests/wphx-php/feed-module-functions.v1.json` and `receipts/compiler/wphx-comp-php-module-function-adapters.v1.json`, with continuation work tracked by `WPHX-COMP-PHP-FEED-EMBED-HTTPS-REMAINDER`. WPHX PHP now emits 19 selected unguarded functions at `wp-includes/feed.php`: `get_bloginfo_rss( $show = '' )`, `bloginfo_rss( $show = '' )`, `get_default_feed()`, `get_wp_title_rss( $deprecated = '&#8211;' )`, `wp_title_rss( $deprecated = '&#8211;' )`, `get_the_title_rss( $post = 0 )`, `the_title_rss()`, `the_excerpt_rss()`, `the_permalink_rss()`, `comments_link_feed()`, `comment_guid( $comment_id = null )`, `get_comment_guid( $comment_id = null )`, `comment_link( $comment = null )`, `get_comment_author_rss()`, `comment_author_rss()`, `comment_text_rss()`, `get_the_content_feed( $feed_type = null )`, `the_content_feed( $feed_type = null )`, and `feed_content_type( $type = '' )`. The gate adds WPHX PHP `@:wp.echo`/`stmt.echo` lowering for selected display wrappers, delegates selected behavior through a stock Haxe PHP helper, preserves native WordPress helper and `apply_filters` timing at the public PHP boundary, and matches a minimized WordPress 7.0 oracle for bloginfo RSS sanitization/conversion/display, default feed normalization, feed title deprecation/filtering/display, title RSS filtering/display, excerpt display filtering, permalink/comment display URL escaping, comment GUID string-or-false behavior through the existing WPHX-211 `NativeValue` boundary, comment author/text filtering/display, feed content filtering/escaping/display, content-type mapping, `empty( '0' )` behavior, reflection ABI, output capture, and filter payloads. Public-shell snapshots still cover 20 shape cases including `wordpress_module_function`; the refreshed compiler gap inventory records 28 Reflaxe-backed WPHX PHP hxmls, 10 stock Haxe PHP implementation hxmls, 15 WPHX PHP evidence manifests, and 23 unsupported-report sites after adding precise echo-body diagnostics. The remaining WPHX-312.04 feed/embed/oEmbed/HTTPS helpers stay open under `WPHX-COMP-PHP-FEED-EMBED-HTTPS-REMAINDER`. The super-progress score stays unchanged because this is a selected compiler/public-shell replacement slice, not full `feed.php` ownership, feed template rendering, installed feed behavior, live provider/network behavior, or retirement of the copied WPHX-312.04 oracle fixture.

2026-07-02 compiler checkpoint: `WPHX-COMP-PHP-FEED-EMBED-HTTPS-REMAINDER` expands the selected feed module-function adapter gate to 22 unguarded original-path `wp-includes/feed.php` functions by adding `html_type_rss()`, `atom_site_icon()`, and `rss2_site_icon()`. The refreshed gate matches the WordPress 7.0 oracle for HTML type normalization/display, Atom site-icon output, RSS2 site-icon output, empty icon URL behavior, and RSS title fallback behavior while keeping reflection ABI, output capture, public-shell snapshots, compiler gap inventory, and receipts current. Category RSS/enclosure/self-link feed helpers plus embed/oEmbed and HTTPS helper clusters remain open under the continuation issue. The super-progress score stays unchanged because this is another selected compiler/public-shell replacement slice, not full `feed.php` ownership, feed template rendering, installed feed behavior, live provider/network behavior, or retirement of the copied WPHX-312.04 oracle fixture.

2026-07-02 compiler checkpoint: `WPHX-COMP-PHP-FEED-EMBED-HTTPS-REMAINDER` expands the selected feed module-function adapter gate to 24 unguarded original-path `wp-includes/feed.php` functions by adding `get_the_category_rss( $type = null )` and `the_category_rss( $type = null )`. The refreshed gate matches the WordPress 7.0 oracle for default feed normalization, RSS2 category CDATA/entity decoding, Atom category scheme/term escaping, RDF `dc:subject` output, category/tag deduplication, display output, filter payloads, reflection ABI, public-shell snapshots, compiler gap inventory, and receipts. Enclosure/self-link feed helpers plus embed/oEmbed and HTTPS helper clusters remain open under the continuation issue. The super-progress score stays unchanged because this is another selected compiler/public-shell replacement slice, not full `feed.php` ownership, feed template rendering, installed feed behavior, live provider/network behavior, or retirement of the copied WPHX-312.04 oracle fixture.

2026-07-02 compiler checkpoint: `WPHX-COMP-PHP-FEED-EMBED-HTTPS-REMAINDER` expands the selected feed module-function adapter gate to 26 unguarded original-path `wp-includes/feed.php` functions by adding `get_self_link()` and `self_link()`. The refreshed gate matches the WordPress 7.0 oracle for `home_url()` parsing, port-aware domain construction, request URI unslashing through the PHP superglobal boundary, URL scheme setting, display escaping, `self_link` filter payloads, reflection ABI, public-shell snapshots, compiler gap inventory, and receipts. Enclosure/feed-build-date helpers plus embed/oEmbed and HTTPS helper clusters remain open under the continuation issue. The super-progress score stays unchanged because this is another selected compiler/public-shell replacement slice, not full `feed.php` ownership, feed template rendering, installed feed behavior, live provider/network behavior, or retirement of the copied WPHX-312.04 oracle fixture.

2026-07-02 compiler checkpoint: `WPHX-COMP-PHP-FEED-EMBED-HTTPS-REMAINDER` expands the selected feed module-function adapter gate to 28 unguarded original-path `wp-includes/feed.php` functions by adding `rss_enclosure()` and `atom_enclosure()`. The refreshed gate matches the WordPress 7.0 oracle for password-required short-circuiting, `get_post_custom()` enclosure meta iteration through a narrow WPHX-211 native-array boundary, malformed RSS enclosure skipping, RSS enclosure URL/length/type output, Atom enclosure URL/length/MIME parsing, `rss_enclosure` and `atom_enclosure` filter payloads, reflection ABI, public-shell snapshots, compiler gap inventory, escape-hatch evidence, and receipts. Feed-build-date helpers plus embed/oEmbed and HTTPS helper clusters remain open under the continuation issue. The super-progress score stays unchanged because this is another selected compiler/public-shell replacement slice, not full `feed.php` ownership, feed template rendering, installed feed behavior, live provider/network behavior, or retirement of the copied WPHX-312.04 oracle fixture.

2026-07-02 compiler checkpoint: `WPHX-COMP-PHP-FEED-EMBED-HTTPS-REMAINDER` expands the selected feed module-function adapter gate to 29 unguarded original-path `wp-includes/feed.php` functions by adding `get_feed_build_date( $format )`. The refreshed gate matches the WordPress 7.0 oracle for post modified-time max selection, comment-feed comment modified-time participation, last-post fallback, invalid-date false behavior, `get_feed_build_date` filter payloads, reflection ABI, public-shell snapshots, compiler gap inventory, escape-hatch evidence, and receipts. This slice records the existing enclosure native-array boundary plus a second WPHX-211 boundary for global `WP_Query` access. Embed/oEmbed and HTTPS helper clusters remain open under the continuation issue. The super-progress score stays unchanged because this is another selected compiler/public-shell replacement slice, not full `feed.php` ownership, feed template rendering, installed feed behavior, live provider/network behavior, or retirement of the copied WPHX-312.04 oracle fixture.

2026-07-02 compiler checkpoint: `WPHX-COMP-PHP-FEED-EMBED-HTTPS-REMAINDER` adds and expands a separate selected HTTPS module-function adapter gate at `manifests/wphx-php/https-module-functions.v1.json` and `receipts/compiler/wphx-comp-php-https-module-functions.v1.json`. WPHX PHP now emits ten unguarded original-path functions across `wp-includes/https-detection.php` and `wp-includes/https-migration.php`: `wp_is_using_https()`, `wp_is_home_url_using_https()`, `wp_is_site_url_using_https()`, `wp_is_https_supported()`, `wp_get_https_detection_errors()`, `wp_is_local_html_output( $html )`, `wp_should_replace_insecure_home_url()`, `wp_replace_insecure_home_url( $content )`, `wp_update_urls_to_https()`, and `wp_update_https_migration_required( $old_url, $new_url )`. The gate matches the WordPress 7.0 oracle for home/site HTTPS URL detection, `site_url` filtering, deterministic HTTPS detection-error short-circuiting, verified/unverified `wp_remote_request()` fallback, `WP_Error` error-array accumulation, response-code/source validation, `wp_is_https_supported()` empty-error behavior, insecure home URL replacement, URL option HTTPS migration and revert behavior, migration-required option updates, local RSD/REST HTML source checks, filter payloads, reflection ABI, public-shell snapshots, compiler gap inventory, escape-hatch evidence, and receipts. The refreshed public-shell snapshot lane covers 23 generated-shape cases, compiler gap inventory records 30 Reflaxe-backed WPHX PHP hxmls, 12 stock Haxe PHP implementation hxmls, 17 WPHX PHP evidence manifests, and 23 unsupported-report sites, and escape-hatch evidence records 97 WPHX-211 boundaries after adding explicit WP_Error, native remote-request args, native errors-array, and strict-false local-source comments. Live HTTPS probing, real TLS verification, Requests transport behavior, embed.php, and oEmbed class/helper clusters remain open under the continuation issue. The super-progress score stays unchanged because this is another selected compiler/public-shell replacement slice, not full HTTPS file ownership, live network/TLS behavior, installed WordPress behavior, or retirement of the copied WPHX-312.04 oracle fixture.

2026-07-02 compiler checkpoint: `WPHX-COMP-PHP-FEED-EMBED-HTTPS-REMAINDER` adds and expands a separate selected embed module-function adapter gate at `manifests/wphx-php/embed-module-functions.v1.json` and `receipts/compiler/wphx-comp-php-embed-module-functions.v1.json`. WPHX PHP now emits twenty-five unguarded original-path `wp-includes/embed.php` functions: `wp_embed_register_handler( $id, $regex, $callback, $priority = 10 )`, `wp_embed_unregister_handler( $id, $priority = 10 )`, `wp_embed_defaults( $url = '' )`, `wp_oembed_get( $url, $args = '' )`, `_wp_oembed_get_object()`, `get_post_embed_url( $post = null )`, `get_oembed_endpoint_url( $permalink = '', $format = 'json' )`, `wp_oembed_ensure_format( $format )`, `_oembed_create_xml( $data, $node = null )`, `wp_oembed_add_provider( $format, $provider, $regex = false )`, `wp_oembed_remove_provider( $format )`, `wp_oembed_register_route()`, `wp_oembed_add_discovery_links()`, `wp_oembed_add_host_js()`, `wp_maybe_enqueue_oembed_host_js( $html )`, `wp_embed_excerpt_more( $more_string )`, `the_excerpt_embed()`, `wp_embed_excerpt_attachment( $content )`, `enqueue_embed_scripts()`, `the_embed_site_title()`, `wp_filter_pre_oembed_result( $result, $url, $args )`, `wp_maybe_load_embeds()`, `wp_embed_handler_youtube( $matches, $attr, $url, $rawattr )`, `wp_embed_handler_audio( $matches, $attr, $url, $rawattr )`, and `wp_embed_handler_video( $matches, $attr, $url, $rawattr )`. The gate matches the WordPress 7.0 oracle for local handler register/unregister delegation through `$wp_embed`, embed default sizing and filters, `WP_oEmbed` singleton creation, `wp_oembed_get()` `get_html()` delegation and raw args forwarding, post embed URL construction, permalink-structure and path-conflict fallback, `post_embed_url` filtering and `sanitize_url()` behavior, oEmbed endpoint URL construction and filters, format normalization, recursive `_oembed_create_xml()` false-return and SimpleXML output behavior, early and post-plugins-loaded provider add/remove registry behavior, oEmbed route controller delegation, discovery link echo output, `wp_head` priority fallback/removal behavior, deprecated empty host-JS marker behavior, conditional `wp-embed` script enqueue detection, embed excerpt more-link formatting, excerpt embed echo/filter behavior, attachment excerpt replacement, embed script action dispatch, embed site title echo/filter behavior, pre-oEmbed local `data2html()` delegation and fallback preservation, default handler loading and callback filters, local YouTube autoembed delegation, local audio/video handler shortcode output, video dimensions, URL escaping, filter payloads, reflection ABI, public-shell snapshots, compiler gap inventory, escape-hatch evidence, adoption CI, and receipts. The refreshed public-shell snapshot lane covers 23 generated-shape cases, compiler gap inventory records 30 Reflaxe-backed WPHX PHP hxmls, 12 stock Haxe PHP implementation hxmls, 17 WPHX PHP evidence manifests, and 23 unsupported-report sites, and escape-hatch evidence records 93 WPHX-211 boundaries after adding explicit native-array, SimpleXML node, post embed URL helper, `$wp_embed`, `WP_oEmbed` singleton, route controller, discovery-link helpers, host-JS enqueue, excerpt helper, autoembed, and provider registry comments. WP_Embed/WP_oEmbed/WP_oEmbed_Controller class method ownership beyond the narrow selected route registration, `get_html()` singleton delegation, handler/provider registry, discovery-link helper calls, host-JS enqueue marker, excerpt helper calls, embed script enqueue action, site-title helper calls, post-embed-url helper calls, pre-oEmbed local response delegation, and autoembed interactions, remote oEmbed fetch/discovery, REST server dispatch, full post embed rendering, installed browser behavior, and live HTTPS probing remain open under the continuation issue. The super-progress score stays unchanged because this is another selected compiler/public-shell replacement slice, not full `embed.php` ownership, remote provider/network behavior, installed WordPress behavior, or retirement of the copied WPHX-312.04 oracle fixture.

2026-07-02 WPHX-312 checkpoint: `WPHX-312.95` adds a machine-readable public PHP shell gap audit at `manifests/wp-core/wphx-312-95-public-shell-gap-audit.v1.json` and `receipts/wp-core/wphx-312-95-public-shell-gap-audit.v1.json`. `WPHX-312.98` reconciles the WPHX-backed HTTP candidate ownership states to `compiler_emitted_original_path_shell`, and `WPHX-312.99` retires the remaining copied `WP_Http::request` branch shells onto the shared WPHX PHP compiler-emitted original-path request shell. The refreshed audit now classifies 89 WPHX-312 ownership manifests into 41 durable generated public shell boundaries, zero WPHX PHP-backed boundaries needing ownership-state reconciliation, zero remaining `WP_Http::request` branch shell gaps, 46 copied-oracle public surfaces, one adapter-contract-only entry, and one inventory entry. Remaining unresolved shell debt stays linked to `WPHX-312.100`, and `WPHX-312.96` still depends on that follow-up before domain closure. The super-progress score stays unchanged because this is bounded public-shell retirement and audit/routing evidence, not full request ownership, live HTTP transport, real cron/mail transport, installed feed/embed/widget/privacy routing, upstream PHPUnit parity, whole-file `WP_Http` ownership, or broad public PHP replacement.

2026-07-02 WPHX-312 checkpoint: `WPHX-312.100` adds a copied public surface follow-up plan at `manifests/wp-core/wphx-312-100-copied-surface-plan.v1.json` and `receipts/wp-core/wphx-312-100-copied-surface-plan.v1.json`. The plan covers all 46 copied-oracle public surfaces from the WPHX-312.95 audit and assigns primary gates to generated adapters (`WPHX-312.101`, 7 surfaces), preserved vendor policy (`WPHX-323`, 6 surfaces), installed routes (`WPHX-312.94`, 19 surfaces), and live transport (`WPHX-312.92`, 14 surfaces), with secondary gates for cron/mail transport (`WPHX-312.93`) and upstream PHPUnit ratchet (`WPHX-312.97`) where applicable. The public-shell audit now excludes WPHX-312 design-plan ownership manifests from runtime/public-shell boundary classification to avoid plan-audit hash cycles. The super-progress score stays unchanged because this is follow-up routing evidence, not generated adapter implementation, installed behavior, live transport, preserved-vendor closure, upstream PHPUnit pass/pass parity, or broad public PHP replacement.

2026-07-02 WPHX-312 checkpoint: `WPHX-312.92` adds a recorded HTTP transport parity gate at `manifests/wp-core/wphx-312-92-http-transport-parity-gate.v1.json` and `receipts/wp-core/wphx-312-92-http-transport-parity-gate.v1.json`. The runner compares vanilla WordPress 7.0 `WP_Http::request` behavior against the WPHX PHP compiler-emitted original-path request shell for seven deterministic Requests-boundary cases covering success/defaults, preemptive responses, invalid/blocked errors, streaming filename and blocking behavior, headers/cookies/SSL/body/redirect/response-size handoff, proxy authentication/TLS handoff, and Requests exception/nonblocking behavior. The refreshed public-shell audit now classifies 90 WPHX-312 ownership manifests into 42 durable generated public shell boundaries, 46 copied-oracle public surfaces, one adapter-contract-only entry, and one inventory entry. The super-progress score stays unchanged because this is recorded Requests-boundary parity, not live network I/O, real DNS/proxy/TLS negotiation, curl/streams execution, installed distribution behavior, upstream PHPUnit parity, whole-file `WP_Http` ownership, or full request ownership.

2026-07-02 WPHX-312 checkpoint: `WPHX-312.93` adds a controlled cron/mail transport installed-style gate at `manifests/wp-core/wphx-312-93-cron-mail-transport-installed-gate.v1.json` and `receipts/wp-core/wphx-312-93-cron-mail-transport-installed-gate.v1.json`. The runner serves oracle/candidate package roots with PHP's built-in server, exercises copied WordPress 7.0 `spawn_cron` against a real local HTTP capture loopback for success, lock-held suppression, and controlled connection failure, and exercises `wp_mail`/PHPMailer through a local SMTP capture server plus controlled SMTP failure. The refreshed public-shell audit now classifies 91 WPHX-312 ownership manifests into 42 durable generated public shell boundaries, 47 copied-oracle public surfaces, one adapter-contract-only entry, and one inventory entry. The super-progress score stays unchanged because this is controlled local transport evidence, not production cron timing races, persistent database/object-cache locks, PHP `mail()` delivery, authenticated/TLS SMTP, DNS/MX behavior, remote server policy, installed database-backed privacy/recovery mail state, upstream PHPUnit parity, generated public PHP replacement, or complete installed behavior.

2026-07-02 WPHX-312 checkpoint: `WPHX-312.94` adds a feed/embed/widget/privacy installed-route gate at `manifests/wp-core/wphx-312-94-feed-embed-widget-privacy-installed-routes-gate.v1.json` and `receipts/wp-core/wphx-312-94-feed-embed-widget-privacy-installed-routes-gate.v1.json`. The runner serves copied WordPress 7.0 oracle/candidate package roots with PHP's built-in server and records deterministic route observations for front-end RSS2 feeds, comments RSS2 feeds, oEmbed REST output, post embed output, RSS widget/sidebar output, RSS block output, privacy request confirmation, and privacy admin list-table output. It documents cross-domain ownership with `WPHX-314` for block rendering and `WPHX-315`/`WPHX-316` for admin/list-table and management state. The refreshed public-shell audit now classifies 92 WPHX-312 ownership manifests into 42 durable generated public shell boundaries, 48 copied-oracle public surfaces, one adapter-contract-only entry, and one inventory entry. The copied-surface plan now covers all 48 copied-oracle surfaces, with 7 generated-adapter, 6 preserved-vendor, 20 installed-route, 14 live-transport, and 1 cron/mail-transport primary gates. The super-progress score stays unchanged because this is deterministic installed-style route evidence, not database-backed installed state, live remote feed/oEmbed network behavior, full admin UI ownership, full block ownership, upstream PHPUnit parity, generated public PHP replacement, or complete installed WordPress behavior.

2026-07-02 WPHX-312 checkpoint: `WPHX-312.97` unblocks the selected WPHX-312 upstream PHPUnit ratchet. The selected `http-cron-mail-core`, `feed-simplepie-core`, `oembed-embed-core`, and `https-privacy-request-core` groups now cover 37 runnable upstream PHPUnit files after removing `tests/phpunit/tests/http/base.php`, which is a support base file that PHPUnit cannot execute directly. `npm run upstream:phpunit-ratchet:provision:check` and `npm run upstream:phpunit-ratchet:check` now record a provisioned environment with 62 vanilla/candidate runs and all four WPHX-312 groups classify vanilla pass / candidate pass with no blocked runtime inputs. The global ratchet still sets `behavior_parity_claimed=false` because three WPHX-314 groups classify vanilla fail / candidate fail, and the candidate assembly is still the pinned WordPress 7.0 worktree without generated/Haxe overlays. The super-progress score stays unchanged because this is upstream ratchet infrastructure and WPHX-312 pass/pass evidence, not generated public PHP replacement, installed database-backed behavior, live network/provider behavior, or full distribution parity.

2026-07-02 WPHX-312 checkpoint: `WPHX-312.101` adds a generated-adapter copied-surface coverage gate at `manifests/wp-core/wphx-312-101-generated-adapter-coverage.v1.json` and `receipts/wp-core/wphx-312-101-generated-adapter-coverage.v1.json`. The gate accounts for all seven copied public surfaces routed to generated adapters by `WPHX-312.100`: six map to existing WPHX PHP compiler-emitted original-path successor candidates (`WPHX-312.50`, `.52`, `.53`, `.55`-.58, and `.60`-.64), and the remaining feed/embed/oEmbed/HTTPS helper cluster from `WPHX-312.04` is routed to the explicit compiler-pressure blocker `WPHX-COMP-PHP-MODULE-FUNCTION-ADAPTERS`. The super-progress score stays unchanged because this is coverage/routing evidence, not new generated PHP ownership, installed behavior, live network/provider behavior, upstream PHPUnit parity, or retirement of copied feed/embed/HTTPS public PHP.

2026-07-02 WPHX-312 checkpoint: `WPHX-312.96` closes the bounded HTTP/cron/mail/feed/embed evidence slice at `receipts/wp-core/wphx-312-domain-closure.v1.json`. Closure covers the surface inventory, typed adapter-contract candidate, copied-oracle fixtures, WPHX PHP-backed HTTP helper/request candidates, recorded HTTP transport parity, controlled cron/mail transport, feed/embed/widget/privacy installed-route observations, public-shell gap audit, copied-surface follow-up plan, and WPHX-312 upstream PHPUnit pass/pass ratchet. This changes WPHX-312 from active to closed only as a bounded evidence domain. Remaining work stays explicitly outside the closure: generated public PHP replacement for copied surfaces, preserved-vendor policy, full generated/Haxe candidate overlays in upstream ratchets, live network/provider behavior, production cron timing, real mail delivery, database-backed installed feed/widget/privacy state, admin/block cross-domain ownership, and broad ecosystem parity. The super-progress score stays unchanged because closure consolidates existing evidence and non-claims rather than adding broad runtime ownership.

2026-07-02 strategy checkpoint: `WPHX-COMP-PHP-BACKEND-STRATEGY-ADR` accepts ADR-015. The project will continue the staged WPHX PHP Adapter IR path for current WordPress public PHP work while acknowledging that the in-repo compiler uses Reflaxe infrastructure. The ADR defines the generated public PHP quality bar, routing rules between stock Haxe PHP, WPHX Adapter IR, reusable WPHX PHP core IR, and future backend promotion, plus criteria for when to request oracle review before a full backend or extracted `reflaxe.php` target. The super-progress score stays unchanged because this is strategy and governance evidence, not new runtime ownership, installed WordPress behavior, or new compiler lowering functionality.

2026-07-02 status reconciliation: `WPHX-313` parent Beads status now matches the already-recorded bounded closure row and `WPHX-313.09` domain-closure receipt. This does not change the super-progress score or broaden claims; generated public PHP replacement, complete upstream media PHPUnit pass/pass parity, real native image-library behavior, remote filesystem transports, database-backed installed media persistence, and updater/admin orchestration remain later work.

2026-06-30 compiler checkpoint: `WPHX-COMP-PHP-GROUP-WP-HTTP-HELPERS` adds one WPHX-generated original-path `wp-includes/class-wp-http.php` shell for the currently proven `WP_Http` parser/header/cookie/IP/redirect/absolute-URL/block-request/redirect-orchestration helper group: `processResponse`, `chunkTransferDecode`, protected `parse_url`, `buildCookieHeader( &$r )`, `processHeaders( $headers, $url = '' )`, `is_ip_address( $maybe_ip )`, `browser_redirect_compatibility( ..., &$options, $original )`, `validate_redirects( $location )`, `make_absolute_url( $maybe_relative_path, $url )`, instance `block_request( $uri )`, and `handle_redirects( $url, $args, $response )`. It passes PHP lint, reflection/default/reference/protected-method checks, all fifteen oracle/candidate behavior probes, WordPress bootstrap-profile checks, and `unsupported=[]` manifest checks. This improves compiler/public-adapter evidence but does not move the super-progress score because it consolidates already-bounded helper claims rather than adding installed WordPress functionality or whole-file `WP_Http` ownership. The next compiler-pressure movement is to use grouped original-path emission as the default replacement path for copied or JS-patched WP_Http public shells, then prove file-segment/template semantics before broad template ownership or full `WP_Http::request` ownership.

2026-06-30 compiler checkpoint: `WPHX-COMP-PHP-PROFILE-ADAPTER-REGISTRY` moves the WordPress-specific `wp-http-*` method body construction out of `WphxPhpCompiler.hx` and into `WphxPhpWordPressAdapters.hx`. The compiler core still owns generic PHP-core IR, feature recording, file/segment planning, bootstrap behavior, manifest writing, and printing; the WordPress profile now returns bounded adapter plans for `processHeaders`, `buildCookieHeader`, `is_ip_address`, `browser_redirect_compatibility`, `validate_redirects`, `make_absolute_url`, `block_request`, and `handle_redirects`. Grouped WP_Http helpers, public-shell snapshots, Haxe formatting, receipts validation, and Beads validation remain green. The super-progress score stays unchanged because this is architecture cleanup and evidence hardening, not new installed WordPress functionality.

2026-06-30 compiler checkpoint: `WPHX-COMP-PHP-TEMPLATE-SEGMENT-MODEL` accepts ADR-005 and records a deterministic WPHX PHP template segment model manifest. It anchors segment kinds, adoption modes, required metadata, and claim gates to the existing F6 admin/theme caller-scope evidence plus the WPHX include-side-effect direct-script evidence. It classifies the admin-style template, theme-style template, nested partial, and direct-script include fixture without claiming generated ownership of existing WordPress mixed PHP/HTML files, HHX/HXX parity for Core templates, arbitrary Haxe expression lowering in PHP caller scope, or whole-file template ownership. The super-progress score stays unchanged.

2026-06-30 compiler checkpoint: `WPHX-COMP-PHP-FIRST-SEGMENT-SHELL` emits the first WPHX compiler-generated original-path admin-style segment shell at `wp-admin/wphx-template-segment-admin.php`. It passes PHP lint, verifies emission manifest `script:template-segment-admin-style` with `unsupported=[]`, checks exact generated shell shape, compares oracle/candidate behavior for the `ABSPATH` guard, literal/template output order, caller-scope local mutation, object mutation, global trace mutation, and native include return array, and adds the segment shell to public-shell snapshot coverage. The super-progress score stays unchanged because this is compiler/template evidence, not installed WordPress functionality or broad template ownership.

2026-06-30 compiler checkpoint: `WPHX-COMP-PHP-NESTED-SEGMENT-SHELL` emits the first WPHX compiler-generated original-path parent template plus nested partial at `wp-admin/wphx-template-nested-parent.php` and `wp-admin/includes/wphx-template-nested-partial.php`. It passes PHP lint, verifies emission manifest `script:template-segment-nested-parent` and `script:template-segment-nested-partial` with `unsupported=[]`, checks exact generated shell shape, compares oracle/candidate behavior for guard returns, ordered parent/partial output, caller-scope local reads and mutation, object mutation, global trace order, nested include return values, repeated include, include_once second-return behavior, and function-scope include locals, and adds both files to public-shell snapshot coverage. The super-progress score stays unchanged because this is compiler/template evidence, not installed WordPress functionality or broad template ownership.

2026-06-30 compiler checkpoint: `WPHX-COMP-PHP-SEGMENT-PLAN-PRINTER` routes the existing admin-style and nested generated segment shells through the first bounded WPHX PHP segment-plan printer. The affected manifests record `segment.plan-printer` with `unsupported=[]`; admin-style and nested oracle/candidate behavior still match, and public-shell snapshots still cover 11 cases. The super-progress score stays unchanged because this is compiler-shape evidence for already minimized fixtures, not installed WordPress functionality, arbitrary Haxe expression lowering, or broad mixed-template ownership.

2026-06-30 compiler checkpoint: `WPHX-COMP-PHP-SEGMENT-MANIFEST` adds structured `segment_plans` metadata to the WPHX PHP emission manifest for the existing generated admin-style and nested segment shell adapters. The focused runners now assert compiler-emitted original path, adapter, adoption mode, segment order, caller-scope facts, include semantics, observable effects, and `unsupported=[]`; behavior remains oracle/candidate identical and public-shell snapshots still cover 11 cases. The super-progress score stays unchanged because this is compiler metadata evidence for bounded fixtures, not installed WordPress functionality or broad template ownership.

2026-06-30 compiler checkpoint: `WPHX-COMP-PHP-SEGMENT-SNAPSHOT` extends the public-shell snapshot lane to assert structured `segment_plans` metadata for the admin-style shell, nested parent, and nested partial cases. The snapshot manifest now records `segment_plan_metadata=true` and `segment_plan_contracts_passed=true` while keeping 11 cases, byte-stable generated PHP, `php -l`, exact excerpt checks, AST-normalized declaration checks, and `unsupported=[]`. The super-progress score stays unchanged because this hardens generated-shape/compiler-metadata evidence, not installed WordPress functionality or broad template ownership.

2026-06-30 compiler checkpoint: `WPHX-COMP-PHP-DIRECT-SCRIPT-SEGMENT-MANIFEST` adds structured `segment_plans` metadata for the bounded `include-side-effects` direct script adapter. The include runner and public-shell snapshots now assert `direct_script_emission`, `script` / `literal_output` / `return_exit` segments, caller-scope locals, include semantics, observable side effects, and `unsupported=[]` while keeping behavior unchanged. The super-progress score stays unchanged because this hardens file-segment metadata for an existing minimized direct-script fixture, not installed WordPress functionality or broad direct-script/template ownership.

2026-06-30 compiler checkpoint: `WPHX-COMP-PHP-SEGMENT-MODEL-COMPILER-EVIDENCE` links compiler-emitted segment plans back into the ADR-005 template segment model. The model runner now reads include-side-effects, admin-style, and nested manifests, validates four emitted plans for original path, adapter, adoption mode, segment order, caller-scope facts, include semantics, observable effects, and `unsupported=[]`, and records seven classified cases so older bridge/context evidence stays distinct from compiler-emitted segment metadata. The super-progress score stays unchanged because this is compiler/model evidence, not installed WordPress functionality or broad mixed-template ownership.

2026-06-30 compiler checkpoint: `WPHX-COMP-PHP-RETIRE-WP-HTTP-PARSE-URL-TEMP-SHELL` rewires the WPHX-312.64 protected `WP_Http::parse_url` candidate to use the WPHX compiler-emitted grouped original-path `wp-includes/class-wp-http.php` shell instead of a copied or runner-patched PHP shell. The runner now compiles `fixtures/wphx-php/wp-http-parser-helpers.hxml`, installs the generated shell into the candidate package, verifies manifest `class:WP_Http` with `unsupported=[]`, verifies `protected static function parse_url($url)`, and keeps the oracle/candidate behavior probe green. The super-progress score stays unchanged because this retires temporary shell scaffolding for an already-bounded helper candidate, not installed WordPress functionality or whole-file `WP_Http` ownership.

2026-06-30 compiler checkpoint: `WPHX-COMP-PHP-RETIRE-WP-HTTP-PROCESS-HEADERS-TEMP-SHELL` rewires the WPHX-312.62 `WP_Http::processHeaders` candidate to use the WPHX compiler-emitted grouped original-path `wp-includes/class-wp-http.php` shell instead of a copied or runner-patched PHP shell. The runner now compiles `fixtures/wphx-php/wp-http-grouped-helpers.hxml` into an isolated per-run WPHX PHP root, installs the generated shell into the candidate package, verifies manifest `class:WP_Http` with `unsupported=[]`, verifies `public static function processHeaders($headers, $url = '')`, and keeps the oracle/candidate behavior probe green for final response block selection, folded-header unfolding, duplicate header arrays, `Set-Cookie` conversion, and native return shape. The super-progress score stays unchanged because this retires temporary shell scaffolding for an already-bounded helper candidate, not installed WordPress functionality or whole-file `WP_Http` ownership.

2026-06-30 compiler checkpoint: `WPHX-COMP-PHP-RETIRE-WP-HTTP-BUILD-COOKIE-HEADER-TEMP-SHELL` rewires the WPHX-312.63 `WP_Http::buildCookieHeader( &$r )` candidate to use the WPHX compiler-emitted grouped original-path `wp-includes/class-wp-http.php` shell instead of a copied or runner-patched PHP shell. The runner now compiles `fixtures/wphx-php/wp-http-grouped-helpers.hxml` into an isolated per-run WPHX PHP root, installs the generated shell into the candidate package, verifies manifest `class:WP_Http` with `unsupported=[]`, verifies `public static function buildCookieHeader(&$r)`, and keeps the oracle/candidate behavior probe green for caller array mutation, scalar-to-`WP_Http_Cookie` upgrading, object preservation, filter payloads, and final Cookie header order. The super-progress score stays unchanged because this retires temporary shell scaffolding for an already-bounded helper candidate, not installed WordPress functionality or whole-file `WP_Http` ownership.

2026-06-30 compiler checkpoint: `WPHX-COMP-PHP-RETIRE-WP-HTTP-ENCODING-TEMP-SHELL` rewires the WPHX-312.50 `WP_Http_Encoding` candidate to use a WPHX compiler-emitted original-path `wp-includes/class-wp-http-encoding.php` public class shell instead of a copied or runner-patched PHP shell. The runner now compiles `fixtures/wphx-php/wp-http-encoding.hxml`, installs the generated shell into the candidate package, verifies manifest `class:WP_Http_Encoding` with `unsupported=[]`, verifies `#[AllowDynamicProperties]` plus all seven public methods, and keeps all six HTTP encoding oracle/candidate behavior cases green. The super-progress score stays unchanged because this retires temporary shell scaffolding for an already-bounded encoding candidate, not live transfer-encoding behavior, upstream HTTP PHPUnit parity, or installed WordPress functionality.

2026-06-30 compiler checkpoint: `WPHX-COMP-PHP-RETIRE-WP-HTTP-PROXY-TEMP-SHELL` rewires the WPHX-312.51 `WP_HTTP_Proxy` candidate to use a WPHX compiler-emitted original-path `wp-includes/class-wp-http-proxy.php` public class shell instead of a copied or runner-patched PHP shell. The runner now compiles `fixtures/wphx-php/wp-http-proxy.hxml`, installs the generated shell into the candidate package, verifies manifest `class:WP_HTTP_Proxy` with `unsupported=[]`, verifies `#[AllowDynamicProperties]` plus all nine public methods, and keeps all seven HTTP proxy oracle/candidate behavior cases green. The super-progress score stays unchanged because this retires temporary shell scaffolding for an already-bounded proxy candidate, not live proxy transport behavior, upstream HTTP PHPUnit parity, `WP_Http::request` ownership, or installed WordPress functionality.

2026-06-30 compiler checkpoint: `WPHX-COMP-PHP-RETIRE-WP-HTTP-RESPONSE-TEMP-SHELL` rewires the WPHX-312.52 `WP_HTTP_Response` candidate to use a WPHX compiler-emitted original-path `wp-includes/class-wp-http-response.php` public class shell instead of a generated temporary PHP shell. The runner now compiles `fixtures/wphx-php/wp-http-response.hxml`, installs the generated shell into the candidate package, verifies manifest `class:WP_HTTP_Response` with `unsupported=[]`, verifies `#[AllowDynamicProperties]`, public `data`/`headers`/`status` properties, constructor defaults, and all nine public methods, and keeps all five HTTP response oracle/candidate behavior cases green. The super-progress score stays unchanged because this retires temporary shell scaffolding for an already-bounded response candidate, not REST dispatch, Requests transport integration, upstream HTTP PHPUnit parity, or installed WordPress functionality.

2026-06-30 compiler checkpoint: `WPHX-COMP-PHP-RETIRE-WP-HTTP-COOKIE-TEMP-SHELL` rewires the WPHX-312.53 `WP_Http_Cookie` candidate to use a WPHX compiler-emitted original-path `wp-includes/class-wp-http-cookie.php` public class shell instead of a copied or runner-patched PHP shell. The runner now compiles `fixtures/wphx-php/wp-http-cookie.hxml`, installs the generated shell into the candidate package, verifies manifest `class:WP_Http_Cookie` with `unsupported=[]`, verifies `#[AllowDynamicProperties]`, public cookie properties, constructor parsing/defaults body, and all five public methods, and keeps all six HTTP cookie oracle/candidate behavior cases green. The super-progress score stays unchanged because this retires temporary shell scaffolding for an already-bounded cookie candidate, not Requests cookie jar semantics, live HTTP behavior, upstream HTTP PHPUnit parity, or installed WordPress functionality.

2026-06-30 compiler checkpoint: `WPHX-COMP-PHP-RETIRE-WP-HTTP-TRANSPORT-SELECTION-TEMP-SHELL` rewires the WPHX-312.66 deprecated `WP_Http` transport-selection candidate to use a WPHX compiler-emitted original-path `wp-includes/class-wp-http.php` shell instead of a copied or runner-patched PHP shell. The runner now compiles `fixtures/wphx-php/wp-http-transport-selection.hxml`, installs the generated shell into the candidate package, verifies manifest `class:WP_Http` with `unsupported=[]`, verifies public `_get_first_available_transport`, private `_dispatch_request`, Haxe transport token/class-name calls, deprecated filter timing, transport-test calls, static transport cache, debug action, and response filter shape, and keeps all six transport selection/dispatch oracle/candidate behavior cases green. The super-progress score stays unchanged because this retires temporary shell scaffolding for an already-bounded deprecated transport-selection candidate, not `WP_Http::request`, live transport execution, upstream HTTP PHPUnit parity, or installed WordPress functionality.

2026-06-30 compiler checkpoint: `WPHX-COMP-PHP-RETIRE-WP-HTTP-REQUEST-NONBLOCKING-TEMP-SHELL` rewires the WPHX-312.65 `WP_Http::request` nonblocking-response candidate to use a WPHX compiler-emitted original-path `wp-includes/class-wp-http.php` shell instead of a copied or runner-patched PHP method body. The runner now compiles `fixtures/wphx-php/wp-http-request-nonblocking.hxml`, installs the generated shell into the candidate package, verifies manifest `class:WP_Http` with `unsupported=[]`, verifies `public function request($url, $args = [])`, `block_request`, Requests dispatch, `WP_HTTP_Requests_Response`, `http_api_debug`, mbstring reset, `http_response` filter shape, and the Haxe nonblocking response call, and keeps the exception/nonblocking oracle/candidate behavior case green. The super-progress score stays unchanged because this retires temporary shell scaffolding for an already-bounded request branch candidate, not full `WP_Http::request`, live Requests network I/O, upstream HTTP PHPUnit parity, whole-file ownership, or installed WordPress functionality.

2026-06-30 compiler checkpoint: `WPHX-COMP-PHP-REQUEST-HELPER-ALIASES` adds named Haxe helper metadata for larger generated public adapters. Repeated `@:wp.haxeHelper` annotations now support a primary helper plus named helper roles; the generated `WP_Http::request` shell uses `safetyOptions` and `streamBlocking` aliases for the redirect-validation hook condition and stream blocking decision while keeping the primary nonblocking response helper. WPHX-312.65 and public-shell snapshots verify the named helper calls, manifest feature flags, PHP lint, byte-stable generation, and `unsupported=[]`. The super-progress score stays unchanged because this is compiler/interface evidence for retiring more copied request shells, not installed WordPress functionality or full `WP_Http::request` ownership.

2026-06-30 compiler checkpoint: `WPHX-COMP-PHP-RETIRE-WP-HTTP-REQUEST-STREAM-BLOCKING-TEMP-SHELL` rewires the WPHX-312.72 `WP_Http::request` stream-blocking candidate to use the WPHX compiler-emitted original-path `wp-includes/class-wp-http.php` request shell instead of a copied or runner-patched PHP method body. The runner now compiles the shared request shell, installs it into the candidate package, verifies manifest `class:WP_Http` with `unsupported=[]`, verifies request IR feature flags and the named stream/safety/nonblocking helper calls, and keeps the streaming oracle/candidate behavior case green. The super-progress score stays unchanged because this retires temporary shell scaffolding for an already-bounded request branch candidate, not full `WP_Http::request`, live streaming transfer behavior, upstream HTTP PHPUnit parity, whole-file ownership, or installed WordPress functionality.

2026-06-30 compiler checkpoint: `WPHX-COMP-PHP-RETIRE-WP-HTTP-REQUEST-SAFETY-TEMP-SHELL` rewires the WPHX-312.67 `WP_Http::request` safety-options candidate to use the WPHX compiler-emitted original-path request shell instead of a copied or runner-patched PHP method body. The runner now compiles the shared request shell, installs it into the candidate package, verifies manifest `class:WP_Http` with `unsupported=[]`, verifies request IR feature flags and named safety/stream/nonblocking helper calls, and keeps all six safety/proxy oracle/candidate behavior cases green. The super-progress score stays unchanged because this retires temporary shell scaffolding for an already-bounded request branch candidate, not full `WP_Http::request`, live network/proxy behavior, upstream HTTP PHPUnit parity, whole-file ownership, or installed WordPress functionality.

2026-06-30 compiler checkpoint: `WPHX-COMP-PHP-RETIRE-WP-HTTP-REQUEST-HEAD-REDIRECTION-TEMP-SHELL` rewires the WPHX-312.83 `WP_Http::request` HEAD-redirection-default candidate to use the WPHX compiler-emitted original-path request shell instead of a copied or runner-patched PHP method body. The runner now compiles the shared request shell, installs it into the candidate package, verifies manifest `class:WP_Http` with `unsupported=[]`, verifies request IR feature flags and named head-redirection/safety/stream/nonblocking helper calls, verifies generated `processHeaders()` and `normalize_cookies()` support, and keeps both HEAD redirection oracle/candidate behavior cases green. The shared shell also adds generic WPHX PHP IR support for `instanceof` and static closure expressions to preserve the upstream `normalize_cookies()` shape without raw PHP bodies. The super-progress score stays unchanged because this retires temporary shell scaffolding for an already-bounded request branch candidate, not full `WP_Http::request`, live redirect/network behavior, upstream HTTP PHPUnit parity, whole-file ownership, or installed WordPress functionality.

2026-06-30 compiler checkpoint: `WPHX-COMP-PHP-RETIRE-WP-HTTP-REQUEST-METHOD-OPTIONS-TEMP-SHELL` rewires the WPHX-312.68 `WP_Http::request` method-options candidate to use the WPHX compiler-emitted original-path request shell instead of a copied or runner-patched PHP method body. The runner now compiles the shared request shell, installs it into the candidate package, verifies manifest `class:WP_Http` with `unsupported=[]`, verifies request IR feature flags and named method/head/safety/stream/nonblocking helper calls, and keeps both method-options oracle/candidate behavior cases green. The super-progress score stays unchanged because this retires temporary shell scaffolding for an already-bounded request branch candidate, not full `WP_Http::request`, method normalization/body serialization, live network behavior, upstream HTTP PHPUnit parity, whole-file ownership, or installed WordPress functionality.

2026-06-30 compiler checkpoint: `WPHX-COMP-PHP-RETIRE-WP-HTTP-REQUEST-REDIRECT-OPTIONS-TEMP-SHELL` rewires the WPHX-312.69 `WP_Http::request` redirect-options candidate to use the WPHX compiler-emitted original-path request shell instead of a copied or runner-patched PHP method body. The runner now compiles the shared request shell, installs it into the candidate package, verifies manifest `class:WP_Http` with `unsupported=[]`, verifies request IR feature flags and named redirect/method/head/safety/stream/nonblocking helper calls, and keeps both redirect-options oracle/candidate behavior cases green. The super-progress score stays unchanged because this retires temporary shell scaffolding for an already-bounded request branch candidate, not full `WP_Http::request`, redirect following, redirect count filtering semantics, live network behavior, upstream HTTP PHPUnit parity, whole-file ownership, or installed WordPress functionality.

2026-06-30 compiler checkpoint: `WPHX-COMP-PHP-RETIRE-WP-HTTP-REQUEST-SSL-OPTIONS-TEMP-SHELL` rewires the WPHX-312.70 `WP_Http::request` SSL-options candidate to use the WPHX compiler-emitted original-path request shell instead of a copied or runner-patched PHP method body. The runner now compiles the shared request shell, installs it into the candidate package, verifies manifest `class:WP_Http` with `unsupported=[]`, verifies request IR feature flags and named ssl/redirect/method/head/safety/stream/nonblocking helper calls, and keeps both SSL-options oracle/candidate cases green. The super-progress score stays unchanged because this retires temporary shell scaffolding for an already-bounded request branch candidate, not full `WP_Http::request`, TLS/certificate validation, live network behavior, upstream HTTP PHPUnit parity, whole-file ownership, or installed WordPress functionality.

2026-06-30 compiler checkpoint: `WPHX-COMP-PHP-RETIRE-WP-HTTP-REQUEST-RESPONSE-SIZE-TEMP-SHELL` rewires the WPHX-312.71 `WP_Http::request` response-size candidate to use the WPHX compiler-emitted original-path request shell instead of a copied or runner-patched PHP method body. The runner now compiles the shared request shell, installs it into the candidate package, verifies manifest `class:WP_Http` with `unsupported=[]`, verifies request IR feature flags and named response-size/ssl/redirect/method/head/safety/stream/nonblocking helper calls, and keeps both response-size oracle/candidate cases green. The super-progress score stays unchanged because this retires temporary shell scaffolding for an already-bounded request branch candidate, not full `WP_Http::request`, actual response truncation, live network behavior, upstream HTTP PHPUnit parity, whole-file ownership, or installed WordPress functionality.

2026-07-01 compiler checkpoint: `WPHX-COMP-PHP-RETIRE-WP-HTTP-REQUEST-STREAM-FILENAME-TEMP-SHELL` rewires the WPHX-312.73 `WP_Http::request` stream-filename candidate to use the WPHX compiler-emitted original-path request shell instead of a copied or runner-patched PHP method body. The runner now compiles the shared request shell, installs it into the candidate package, verifies manifest `class:WP_Http` with `unsupported=[]`, verifies request IR feature flags and named stream-filename/response-size/ssl/redirect/method/head/safety/stream/nonblocking helper calls, and keeps the stream-filename oracle/candidate case green. The super-progress score stays unchanged because this retires temporary shell scaffolding for an already-bounded request branch candidate, not full `WP_Http::request`, filename path normalization, actual streaming file I/O, live network behavior, upstream HTTP PHPUnit parity, whole-file ownership, or installed WordPress functionality.

2026-07-01 compiler checkpoint: `WPHX-COMP-PHP-RETIRE-WP-HTTP-REQUEST-INVALID-URL-TEMP-SHELL` rewires the WPHX-312.74 `WP_Http::request` invalid-url candidate to use the WPHX compiler-emitted original-path request shell instead of a copied or runner-patched PHP method body. The runner now compiles the shared request shell, installs it into the candidate package, verifies manifest `class:WP_Http` with `unsupported=[]`, verifies request IR feature flags and named invalid-url/stream-filename/response-size/ssl/redirect/method/head/safety/stream/nonblocking helper calls, and keeps the invalid-and-blocked oracle/candidate case green. The super-progress score stays unchanged because this retires temporary shell scaffolding for an already-bounded request branch candidate, not full `WP_Http::request`, URL parsing or validation internals, live network behavior, upstream HTTP PHPUnit parity, whole-file ownership, or installed WordPress functionality.

2026-07-01 compiler checkpoint: `WPHX-COMP-PHP-RETIRE-WP-HTTP-REQUEST-BLOCKED-REQUEST-TEMP-SHELL` rewires the WPHX-312.75 `WP_Http::request` blocked-request candidate to use the WPHX compiler-emitted original-path request shell instead of a copied or runner-patched PHP method body. The runner now compiles the shared request shell, installs it into the candidate package, verifies manifest `class:WP_Http` with `unsupported=[]`, verifies request IR feature flags and named blocked-request/invalid-url/stream-filename/response-size/ssl/redirect/method/head/safety/stream/nonblocking helper calls, and keeps the invalid-and-blocked oracle/candidate case green. The super-progress score stays unchanged because this retires temporary shell scaffolding for an already-bounded request branch candidate, not full `WP_Http::request`, `block_request` internals, URL parsing or validation internals, live network behavior, upstream HTTP PHPUnit parity, whole-file ownership, or installed WordPress functionality.

2026-07-01 compiler checkpoint: `WPHX-COMP-PHP-RETIRE-WP-HTTP-REQUEST-PREEMPTIVE-RESPONSE-TEMP-SHELL` rewires the WPHX-312.76 `WP_Http::request` preemptive-response candidate to use the WPHX compiler-emitted original-path request shell instead of a copied or runner-patched PHP method body. The runner now compiles the shared request shell, installs it into the candidate package, verifies manifest `class:WP_Http` with `unsupported=[]`, verifies request IR feature flags and named preemptive-response/blocked-request/invalid-url/stream-filename/response-size/ssl/redirect/method/head/safety/stream/nonblocking helper calls, and keeps the preemptive-response oracle/candidate case green. The super-progress score stays unchanged because this retires temporary shell scaffolding for an already-bounded request branch candidate, not full `WP_Http::request`, `pre_http_request` filter semantics, returned response shape validation, live network behavior, upstream HTTP PHPUnit parity, whole-file ownership, or installed WordPress functionality.

2026-06-30 compiler checkpoint: `WPHX-COMP-PHP-ADAPTER-TEMPLATE-PROVENANCE` turns the large `WP_Http::request` public adapter body from inline Haxe string concatenation into a compiler-owned adapter template with manifest provenance. WPHX PHP emission manifests now record `adapter_templates`; the WPHX-312.65 runner requires the request template path, SHA-256, `HELPER_CLASS` placeholder, ownership, and upstream reference before accepting the generated shell. AGENTS.md now forbids large inline raw public adapter bodies unless they are structured IR or provenance-tracked compiler adapter templates. The super-progress score stays unchanged because this hardens compiler architecture and evidence quality, not installed WordPress functionality or broader request ownership.

2026-06-30 compiler checkpoint: `WPHX-COMP-PHP-COOKIE-CONSTRUCTOR-TEMPLATE-PROVENANCE` retires the next large inline raw public adapter body by moving `WP_Http_Cookie::__construct` into `src/wphx/compiler/php/templates/wordpress/wp-http-cookie-construct-body.php.template`. The WPHX-312.53 runner now requires manifest `adapter_templates` evidence for the cookie constructor template path, SHA-256, empty-placeholder list, ownership, and upstream reference before accepting the compiler-emitted `wp-includes/class-wp-http-cookie.php` shell, while keeping all six cookie candidate cases and public-shell snapshots green. The super-progress score stays unchanged because this hardens compiler architecture and evidence quality, not broader cookie ownership, Requests cookie jar semantics, live HTTP behavior, or installed WordPress functionality.

2026-06-30 compiler checkpoint: `WPHX-COMP-PHP-TRANSPORT-RAW-BLOCK-RETIREMENT` removes the final inline raw PHP adapter bodies from `WphxPhpWordPressAdapters.hx`: deprecated `WP_Http::_get_first_available_transport()` and private `_dispatch_request()` now render from compiler-owned templates with manifest path/hash/placeholders/ownership/upstream evidence. The WPHX-312.66 runner requires both template records before accepting the generated transport shell, and all six transport selection/dispatch candidate cases plus public-shell snapshots remain green. The super-progress score stays unchanged because this hardens compiler architecture and evidence quality, not live transport execution, full dispatch ownership, whole-file `WP_Http`, or installed WordPress functionality.

2026-06-30 compiler checkpoint: `WPHX-COMP-PHP-ADAPTER-RAW-BLOCK-GUARD` adds `npm run wphx:php:adapter-raw-blocks:check` to precommit and records `manifests/wphx-php/adapter-raw-block-policy.v1.json`. The guard scans `WphxPhpWordPressAdapters.hx` and fails unless every `PhpRawBlock` receives `rendered.code`, preserving the compiler-owned template boundary while blocking new inline PHP string bodies in the WordPress profile. The super-progress score stays unchanged because this is a compiler policy guard, not new WordPress runtime parity.

2026-06-30 compiler checkpoint: `WPHX-COMP-PHP-ADAPTER-TEMPLATE-REFERENCE-GUARD` extends the raw-block guard with template-reference validation. The check now fails unless every WordPress-profile `renderTemplate` call has static adapter/path arguments, every referenced template exists under the compiler-owned WordPress template directory, every compiler-owned WordPress adapter template is referenced by `WphxPhpWordPressAdapters.hx`, and adapter template names are unique. It records `receipts/compiler/wphx-comp-php-adapter-template-reference-policy.v1.json`; the super-progress score stays unchanged because this is compiler policy/evidence hygiene, not new WordPress runtime parity.

2026-06-30 compiler checkpoint: `WPHX-COMP-PHP-ADAPTER-TEMPLATE-CONTENT-GUARD` extends the same policy guard to adapter-template contents. The check now rejects invalid `{{PLACEHOLDER}}` syntax, verifies each template's placeholders match static `renderTemplate` replacement metadata, renders each template with deterministic lint stand-ins, wraps the rendered body in a method-scope PHP fixture, and runs `php -l`. It records `receipts/compiler/wphx-comp-php-adapter-template-content-policy.v1.json`; the super-progress score stays unchanged because this is template syntax/provenance hygiene, not runtime parity or new installed WordPress functionality.

2026-06-30 compiler checkpoint: `WPHX-COMP-PHP-DISPATCH-ADAPTER-IR-PROMOTION` promotes private `WP_Http::_dispatch_request()` from a compiler-owned raw adapter template into structured WPHX PHP core IR. The compiler core now supports method-scope static locals and dynamic class construction for bounded adapter bodies; the WPHX-312.66 runner verifies the dispatch template is absent, dispatch IR features are present, the generated shell keeps static transport caching / `WP_Error` / `http_api_debug` / `http_response` shape, and all six transport selection/dispatch oracle/candidate cases still match. The active WordPress-profile raw-block/template count drops from 4 to 3. The super-progress score stays unchanged because this reduces compiler escape-hatch surface but does not add installed WordPress functionality or live transport ownership.

2026-06-30 compiler checkpoint: `WPHX-COMP-PHP-TRANSPORT-GET-FIRST-IR-PROMOTION` promotes public `WP_Http::_get_first_available_transport()` from a compiler-owned raw adapter template into structured WPHX PHP core IR. The WPHX-312.66 runner now verifies both transport helper adapter templates are absent, get-first and dispatch IR features are present, the generated shell keeps deprecated filter / helper-token / static transport-test / static-cache / debug/filter shape, and all six transport selection/dispatch oracle/candidate cases still match. The active WordPress-profile raw-block/template count drops from 3 to 2. The super-progress score stays unchanged because this reduces compiler escape-hatch surface but does not add installed WordPress functionality or live transport ownership.

2026-06-30 compiler checkpoint: `WPHX-COMP-PHP-COOKIE-CONSTRUCTOR-IR-PROMOTION` promotes `WP_Http_Cookie::__construct` from a compiler-owned raw adapter template into structured WPHX PHP core IR. The compiler core now supports bare returns, list assignment, dynamic object properties, null coalescing, and ternary expressions for bounded adapter bodies; the WPHX-312.53 runner verifies the cookie constructor template is absent, required IR features are present, the generated shell keeps constructor parsing/defaults and dynamic Set-Cookie attribute shape, and all six cookie oracle/candidate cases still match. The active WordPress-profile raw-block/template count drops from 2 to 1. The super-progress score stays unchanged because this reduces compiler escape-hatch surface but does not add broader cookie ownership, Requests cookie jar semantics, live HTTP behavior, or installed WordPress functionality.

2026-06-30 compiler checkpoint: `WPHX-COMP-PHP-REQUEST-NONBLOCKING-IR-PROMOTION` promotes the bounded `WP_Http::request` nonblocking public adapter from the final compiler-owned raw adapter template into structured WPHX PHP core IR. The compiler core now supports `try/catch` and PHP constant expressions for bounded adapter bodies; the WPHX-312.65 runner verifies the request adapter template is absent, request IR features are present, the generated shell keeps request defaults, filters, validation, stream checks, Requests dispatch, exception conversion, mbstring reset, debug action, nonblocking helper delegation, and response filter shape, and the deterministic nonblocking oracle/candidate case still matches. The active WordPress-profile raw-block/template count drops from 1 to 0. The super-progress score stays unchanged because this retires compiler escape-hatch surface but does not claim full `WP_Http::request`, live Requests network I/O, redirect following, transport execution, upstream HTTP PHPUnit parity, installed distribution ownership, or whole-file `WP_Http`.

## PRD Milestone Matrix

| External ref | Scope | Beads status | Beads child progress | Haxe/functionality state | Next required movement |
| --- | --- | --- | ---: | --- | --- |
| `WPHX-000` | Program governance and baseline | open | 17/17 baseline children closed, plus governance follow-ups through `WPHX-000.06` | Control-plane baseline is usable; progress-matrix governance, the idiomatic-Haxe source rule, the typed JSON/Unknown boundary rule, and the Haxe module/class/function documentation threshold rule are documented; governance remains open for program-level decisions | Keep PRD, AGENTS, operations docs, locks, and progress matrix current |
| `WPHX-100` | PHP target feasibility | closed | 9/9 | Stock PHP target feasibility gates passed | Reopen only through compiler-pressure evidence |
| `WPHX-200` | PHP ABI, runtime, and linker | closed | 8/8 | ABI extraction, macro contracts, boundary types, facades, linker, public types, source maps, and escape-hatch audit exist | Use this foundation for each migrated Core slice |
| `WPHX-300` | WordPress PHP core | open | 8/8 original direct children closed; WPHX-306, WPHX-307, WPHX-308, WPHX-309, WPHX-310, WPHX-312, and WPHX-313 are bounded closed; WPHX-314 is active; WPHX-315 through WPHX-320 remain seeded follow-up domains | Bootstrap, hooks, error/formatting/escaping/KSES, options/cache, wpdb, REST, multisite, first-party PHP manifest/split closure, users/auth adapter-contract/domain-closure evidence, bounded posts/query domain closure, bounded taxonomy/comment domain closure, bounded rewrite/routing/canonical/template domain closure, bounded theme/theme JSON/template hierarchy domain closure, bounded HTTP/cron/mail/feed/embed domain closure, bounded media/filesystem/upload domain closure, WPHX-314 blocks/interactivity surface inventory, first typed adapter-contract candidate, parser/render oracle fixture, supports/bindings oracle fixture, patterns registry oracle fixture, block hooks insertion oracle fixture, style engine oracle fixture, HTML API tag processor oracle fixture, interactivity API oracle fixture, core block renderer oracle fixture, selected upstream PHPUnit ratchet scope, and installed-style WPHX-314 block rendering gate, plus WPHX-312 HTTP/cron/mail/feed/embed surface inventory, first typed WPHX-312 adapter-contract evidence, side-effect-safe HTTP/cron/mail oracle-source fixture, side-effect-safe feed/embed/HTTPS oracle-source fixture, AI HTTP fake-transport oracle-source fixture, trackback endpoint oracle-source fixture, privacy request mail oracle-source fixture, remote feed/oEmbed fake-HTTP oracle-source fixture, installed-style WPHX-312 HTTP gate, WPHX-312 selected upstream PHPUnit pass/pass ratchet, WPHX-312 privacy request admin-state oracle-source fixture, WPHX-312 cron dispatch/lock oracle-source fixture, WPHX-312 PHPMailer setup oracle-source fixture, WPHX-312 OPML links output oracle-source fixture, WPHX-312 RSS2 feed template oracle-source fixture, WPHX-312 Atom feed template oracle-source fixture, WPHX-312 RSS2 comments feed template oracle-source fixture, WPHX-312 Atom comments feed template oracle-source fixture, WPHX-312 RDF feed template oracle-source fixture, WPHX-312 RSS 0.92 feed template oracle-source fixture, deprecated embed/oEmbed/RSS fixtures, Magpie/SimplePie fixtures, RSS widget/block fixtures, HTTP proxy/Requests/IXR/encoding/response/cookie/parser/API wrapper fixtures, recorded HTTP transport parity, controlled cron/mail transport, installed feed/embed/widget/privacy route evidence, public-shell audit, copied-surface plan, and bounded WP_Http helper plus request option Haxe parity candidates; broad Core implementation remains open through remaining seeded follow-up domains | Continue WPHX-314 with provisioned upstream PHPUnit pass/pass execution, broader installed block-rendering gates, Haxe-owned candidate promotion, and generated public PHP adapter gates; route WPHX-312 follow-up debt through WPHX-312.101, WPHX-323, WPHX-315/WPHX-316, and later distribution/e2e gates |
| `WPHX-400` | genes-ts/browser platform | open | 0/0 | Planning/feasibility track, not active implementation yet | Seed browser platform tasks and prove genes-ts package, React/TSX, exports, source maps, and bundling gates |
| `WPHX-500` | Gutenberg packages | not seeded | 0/0 | Do not start broad package translation yet | Create after `WPHX-400` package/React feasibility justifies parallelization and an ADR confirms repo protocol |
| `WPHX-600` | WordPress classic JS and browser vendors | not seeded | 0/0 | Not active | Start after browser platform gates expose the right package/runtime boundaries |
| `WPHX-700` | Parity, security, performance, and distribution | open | 10/10 current children closed after `WPHX-700.10` | Early generated-PHP, live parity, packaged ABI, upstream PHPUnit ratchet, and CI gates exist. `WPHX-312.97` refreshes the provisioned selected upstream PHPUnit ratchet to 31 groups / 62 vanilla-candidate runs; WPHX-312 and WPHX-313 groups classify pass/pass, while remaining WPHX-314 baseline failures classify vanilla fail / candidate fail rather than candidate regressions. Full distribution/security/performance closure remains open | Keep ratcheting toward installed-system parity, full upstream suites, ecosystem fixtures, and nonfunctional gates |
| `WPHX-800` | Codex and Beads program operations | closed | 7/7 | Program workflow, task packs, receipts, multi-agent, and Dolt backup/sync are operational | Maintain via `WPHX-000` follow-ups unless a new operations epic is needed |
| `WPHX-COMP` | Compiler-pressure and custom target queue | active focus | `WPHX-COMP-PHP-USABLE` is closed with 5/5 child gates passed through gap inventory, runtime/std strategy, reusable core lowering, whole-file pilot, and adoption CI | WPHX PHP uses Reflaxe infrastructure and emits original-path public adapters for selected global functions/classes/scripts and bounded WordPress HTTP public shells while stock Haxe PHP remains a bounded private implementation/stdphp oracle. The adoption CI gate records 27 WPHX PHP public-adapter hxmls, 9 stock Haxe PHP private-output hxmls, 46 helper bridge sites, 18 bootstrap sites, 39 WordPress-profile method adapters, 5 script adapters, 18 unsupported typed-lowering report sites, 6 runner copy/install surfaces, 14 passing WPHX PHP manifests, and 7 available stock Haxe PHP reference files. The current WordPress profile has zero `PhpRawBlock` occurrences and zero active adapter templates. WPHX PHP now has no-helper typed statement lowering evidence for `if`, `while`, `break`, and `continue`, ADR-017 stock-target oracle evidence for boot/autoload, arrays/maps/iterators, closures, exceptions, strings/JSON, source maps, and source comments, a minimized `whole_file_owned` real WordPress file gate for `wp-includes/class-http.php`, and a passing adoption CI decision that unblocks bounded parallel Core work. It still does not claim full arbitrary-Haxe backend maturity, WPHX runtime/std ownership, `WP_Http`/`class-wp-http.php` ownership, live transport ownership, installed WordPress functionality, or Gutenberg/browser ownership. | Resume bounded Core slices under the adoption CI rules; request oracle review before fully abandoning stock Haxe PHP or claiming mature `reflaxe.php` scope |
| `WPHX-MTGT` | Future multi-target profiles, including browser/WASM, Go, Rust, and Playground-adjacent research | not seeded | 0/0 | Explicit long-term option only; `back2dos/wasmix` is the current WASM reference/base candidate; `fullofcaffeine/reflaxe.go` and `fullofcaffeine/reflaxe.rust` are current Go/Rust references; broader Haxe-supported/custom targets remain possible | Start only after PHP parity and browser platform evidence can define bounded compatibility profiles and the PHP plugin/theme adapter story |

## WordPress PHP Core Detail

`WPHX-300` has closed the original 8 direct milestone gates through WPHX-322, and WPHX-322 seeded WPHX-306 through WPHX-320 as explicit source-domain follow-up owners. Treat this as **manifest ownership/split progress**, not as "WordPress Core is ported."

| Slice | Status | Current interpretation |
| --- | --- | --- |
| `WPHX-301` Bootstrap/load order/constants/environment | closed | Bootstrap trace and load-order gates exist |
| `WPHX-302` Hooks/plugin API | closed | Hook surface promoted through distribution-surface evidence |
| `WPHX-303` Error/deprecation/formatting/escaping/KSES | closed | Multiple Haxe-owned decision slices and security corpus gates exist |
| `WPHX-304` Options/transients/object cache | closed | Option/cache candidate gates and fixtures exist |
| `WPHX-305` wpdb/database abstraction | closed | Strong wpdb vertical slice exists, including native mysqli and packaged ABI gates; still not evidence that every related Core DB path is Haxe-owned |
| `WPHX-306` Users/roles/capabilities/auth/cookies/nonces | closed | Domain closure receipt exists across the surface inventory, first typed Haxe auth adapter-contract candidate, capability/role fixture, auth-cookie/nonce fixture, password/application-password fixture, runtime ABI fixture, and installed-style HTTP auth gate. `WPHX-306.07` covers login/logout, Set-Cookie headers, cookie-authenticated profile access, AJAX nonce success/failure, REST-style application-password Basic auth, profile application-password creation, package-boundary reflection, and Haxe contract presence. Public PHP auth files are still copied oracle source; generated public PHP replacement, live MySQL-backed installation, selected upstream auth PHPUnit groups, and full external plugin/admin-screen corpus remain later distribution work. |
| `WPHX-307` Posts/metadata/revisions/WP_Query | closed | Domain closure receipt exists across the surface inventory, typed Haxe posts/query, CRUD/status, metadata/cache, and revision adapter-contract candidates, deterministic WP_Query runtime ABI/query-state fixture, live WP_Query SQL/result fixture, MySQL/MariaDB/PHP DB-client runtime matrix, selected upstream PHPUnit pass/pass evidence, and installed-style packaged HTTP gate. Closure is bounded: public PHP post/query files are still copied oracle source, generated public PHP replacement is not claimed, full database-backed installed post behavior remains later distribution work, and this does not close the whole WordPress posts subsystem beyond the recorded evidence. |
| `WPHX-308` Taxonomy/terms/comments | closed | Domain closure receipt exists across the surface inventory, first typed Haxe taxonomy/comment adapter-contract candidate, deterministic CRUD oracle fixture, count/cache invalidation fixture, query-state fixture, live MySQL/MariaDB plus PHP DB-client SQL/result matrix, selected upstream PHPUnit taxonomy/comment pass/pass evidence, and installed-style packaged HTTP gate. `WPHX-308.09` covers taxonomy registration, term insert/meta/object assignment/count/cache observations, pending comment insertion, comment approval, comment meta, front taxonomy filtering, and comment query/moderation filtering over oracle and candidate packages. Closure is bounded: public PHP taxonomy/comment files are still copied oracle source, generated public PHP replacement is not claimed, full database-backed installed term/comment behavior remains later distribution work, and this does not close the whole WordPress taxonomy/comment subsystem beyond the recorded evidence. |
| `WPHX-309` Rewrite/routing/canonical URLs/templates | closed | Domain closure receipt exists across the surface inventory, first typed Haxe routing/template adapter-contract candidate, deterministic routing/link oracle fixture, canonical/template-loader oracle fixture, installed-style packaged HTTP gate, selected upstream PHPUnit ratchet group scope, and provisioned upstream rewrite/routing/canonical/link/template pass/pass evidence. Closure is bounded: public PHP routing/template files are still copied oracle source, generated public PHP replacement is not claimed, full database-backed front-end routing/canonical/template behavior remains later distribution work, and this does not close the whole WordPress routing/template subsystem beyond the recorded evidence. |
| `WPHX-310` Themes/theme JSON/template hierarchy | closed | Domain closure receipt exists across the surface inventory, first typed Haxe theme/template adapter-contract candidate, theme support/template oracle fixture, WP_Theme_JSON/global styles fixture, resolver/global-styles wrapper/cache fixture, customizer/widget/nav-menu/admin-theme narrowed inventory, widget/sidebar fixture, nav-menu fixture, Customizer manager/settings fixture, packaged installed-style theme front-end/admin gate, and selected upstream theme/template/theme-json/block-template/widget/nav-menu/Customizer pass/pass evidence. Closure is bounded: public PHP theme/customizer/widget/nav-menu files are still copied oracle source, generated public PHP replacement is not claimed, full database-backed installed theme behavior remains later distribution work, and this does not close the whole WordPress theme subsystem beyond the recorded evidence. |
| `WPHX-311` REST API/schema | closed | Settings and REST server decision slices are typed Haxe strategy candidates; packaged, installed-browser, DB-backed, and cross-origin REST gates cover the active transport surface; `WPHX-311.10a` hardens the DB-backed browser harness against automatic favicon/network-idle fetch races; `WPHX-700.10` fixes upstream PHPUnit REST group selection and records REST settings/server groups as vanilla pass / candidate pass |
| `WPHX-312` HTTP/cron/mail/feeds/embeds | closed | Domain closure receipt exists across the surface inventory, first typed Haxe HTTP/cron/mail/feed/embed adapter-contract candidate, copied-oracle/candidate fixtures for HTTP helpers, cron, mail, feeds, OPML, trackbacks, privacy mail/admin state, oEmbed/embed, Magpie/SimplePie, RSS widgets/blocks, and HTTP wrappers, WPHX PHP-backed generated HTTP helper/request candidates, recorded HTTP transport parity, controlled cron/mail transport, installed feed/embed/widget/privacy route observations, public-shell gap audit, copied-surface follow-up plan, and provisioned upstream PHPUnit pass/pass classifications for the four selected WPHX-312 groups covering 37 runnable files. Closure is bounded: generated public PHP replacement for copied surfaces, preserved-vendor policy, generated/Haxe candidate overlays in upstream ratchets, live network/provider behavior, production cron timing, real mail delivery, database-backed installed feed/widget/privacy state, admin/block cross-domain ownership, and broad ecosystem parity remain later work. |
| `WPHX-312.18` Atom comments feed detail | closed | Adds an HTTP-observed copied-oracle/candidate PHP fixture with 5 deterministic cases and 25 covered symbols across `feed-atom-comments.php`, Atom comments content-type headers, default/singular/search title branches, namespace/head/entry actions, default/search/singular self and alternate links, author URI output, protected-comment content, top-level and parent-comment threading references, post lookup, title filtering, GUIDs, dates, and entry hook arguments through a deterministic auto-prepended comment-loop stub. The same WPHX-312 bounded non-claims above still apply. |
| `WPHX-312.19` RDF feed detail | closed | Adds an HTTP-observed copied-oracle/candidate PHP fixture with 5 deterministic cases and 21 covered symbols across `feed-rdf.php`, RDF content-type headers, root namespace and header hooks, update period/frequency filters, channel metadata, `rdf:Seq` item references, `rewind_posts` two-pass loop behavior, post item rendering, author/date/category output, excerpt/content branches, empty content output, and item hook arguments through a deterministic auto-prepended feed-loop stub. The same WPHX-312 bounded non-claims above still apply. |
| `WPHX-312.20` RSS 0.92 feed detail | closed | Adds an HTTP-observed copied-oracle/candidate PHP fixture with 5 deterministic cases and 12 covered symbols across `feed-rss.php`, RSS 0.92 content-type headers, channel metadata, build date/language output, `rss_head`, post loop rendering, title/excerpt/link output, special-character title escaping, empty excerpt output, no-post channel output, and `rss_item` hook arguments through a deterministic auto-prepended feed-loop stub. The same WPHX-312 bounded non-claims above still apply. |
| `WPHX-312.21` Deprecated embed/RSS wrapper detail | closed | Adds an HTTP-observed copied-oracle/candidate PHP fixture with 4 deterministic cases and 10 covered symbols across `embed-template.php`, `rss-functions.php`, `_deprecated_file`, `basename( __FILE__ )`, `ABSPATH`, `WPINC`, `require`, `require_once`, `theme-compat/embed.php` handoff, and `rss.php` handoff through deterministic required-file stubs. It does not claim real theme-compat embed rendering, MagpieRSS/SimplePie behavior, installed routing, or generated public PHP replacement. |
| `WPHX-312.22` MagpieRSS parser detail | closed | Adds a PHP CLI copied-oracle/candidate fixture with 5 deterministic cases and 19 covered symbols across `rss.php`, `MagpieRSS`, RSS2/RDF/Atom parsing, channel/item normalization, namespace content, RDF `about`, Atom alternate links, `_response_to_rss` etag/last-modified handoff, `parse_w3cdtf`, HTTP status helpers, and pure `RSSCache` filename/serialize/unserialize helpers. It also records the `rss.php` deprecation and `load_feed_engine` action, while explicitly not claiming remote fetch, transient cache, `wp_rss`/`get_rss` display output, widgets, installed routing, or generated public PHP replacement. |
| `WPHX-312.23` MagpieRSS fetch/cache detail | closed | Adds a PHP CLI copied-oracle/candidate fixture with 6 deterministic cases and 20 covered symbols across `rss.php`, `fetch_rss`, `_fetch_remote_file`, `_response_to_rss`, WP HTTP response/error conversion stubs, cache-off fetch, cache-miss transient population, cache-hit short-circuit with `from_cache`, and `RSSCache` transient `set`/`get`/`check_cache` key behavior. It records that the legacy STALE/304 branch is not naturally reachable because WordPress 7.0 `RSSCache::check_cache` only returns `HIT` or `MISS`; it does not claim live network I/O, display helpers, widgets, installed routing, or generated public PHP replacement. |
| `WPHX-312.24` MagpieRSS display helper detail | closed | Adds a PHP CLI copied-oracle/candidate fixture with 5 deterministic cases and 16 covered symbols across `rss.php`, `wp_rss`, `get_rss`, cached `fetch_rss` handoff, `RSSCache::check_cache`/`get`, deterministic escaping and translation stubs, list slicing, success output, and failure output/return behavior. It records the legacy display quirks that `wp_rss` wraps output in `<ul>` and escapes URL/stripped description/title, while `get_rss` emits bare `<li>` rows, escapes only title text, and preserves raw link/description attributes; it does not claim live network I/O, widgets, installed routing, SimplePie behavior, database-backed feeds, or generated public PHP replacement. |
| `WPHX-312.25` RSS widget helper detail | closed | Adds a PHP CLI copied-oracle/candidate fixture with 5 deterministic cases and 20 covered symbols across `widgets.php`, `wp_widget_rss_output`, `wp_widget_rss_process`, deterministic `fetch_feed`/SimplePie-like feed stubs, error visibility for admin/manage-options, empty-feed fallback output, summary/date/author item rendering, link trimming, item-count clamping, URL/title sanitization, feed permalink normalization, and successful feed destruction. It does not claim `WP_Widget_RSS` class runtime, installed widget registration/sidebar rendering, admin form HTML, live network feeds, database-backed widgets, or generated public PHP replacement. |
| `WPHX-312.26` WP_Widget_RSS class detail | closed | Adds a PHP CLI copied-oracle/candidate fixture with 5 deterministic cases and 22 covered symbols across `class-wp-widget-rss.php`, `WP_Widget_RSS::__construct`, `widget`, `update`, and `form`, using deterministic `WP_Widget`, feed, escaping, filter, theme-support, URL, lazy-loading, and RSS widget helper stubs. It covers constructor metadata, widget error/empty/self-URL guards, URL/permalink normalization, title/feed-link wrapping, HTML5 navigation wrapper, delegation to `wp_widget_rss_output`, update `testurl` computation, and form defaulting with widget number; it does not claim installed widget registration/sidebar rendering, helper internals covered by `WPHX-312.25`, admin form helper HTML, live network feeds, database-backed widgets, or generated public PHP replacement. |
| `WPHX-312.27` RSS block renderer detail | closed | Adds a PHP CLI copied-oracle/candidate fixture with 5 deterministic cases and 21 covered symbols across `blocks/rss.php`, `render_block_core_rss`, `register_block_core_rss`, deterministic `fetch_feed`/SimplePie-like feed stubs, self-feed loop-prevention placeholder, `WP_Error` and empty-feed placeholders, linked item rendering with `target`/`rel`, date offset/formatting, author byline, excerpt trimming, grid/date/author/excerpt wrapper classes, `get_block_wrapper_attributes`, `register_block_type_from_metadata`, and `add_action` registration. It does not claim installed block editor behavior, client-side block rendering, live network feeds, database-backed feeds, full block registry integration, or generated public PHP replacement. |
| `WPHX-312.28` Deprecated oEmbed wrapper detail | closed | Adds a PHP CLI copied-oracle/candidate fixture with 3 deterministic cases and 8 covered symbols across `class-oembed.php`, `_deprecated_file`, `basename( __FILE__ )`, `ABSPATH`, `WPINC`, `require_once`, `class-wp-oembed.php` handoff, and `WP_oEmbed` class availability through a deterministic required-file stub. It covers single include, double include, and wrapper `require_once` idempotency; it does not claim WP_oEmbed provider internals, REST oEmbed controller behavior, live network/discovery, installed routing, or generated public PHP replacement. |
| `WPHX-312.29` oEmbed provider registry detail | closed | Adds a PHP CLI copied-oracle/candidate fixture with 5 deterministic cases and 15 covered symbols across `class-wp-oembed.php`, `WP_oEmbed::__construct`, built-in providers, early provider add/remove queues, `oembed_providers`, regex/wildcard `get_provider` matching, `__call` compatibility access to private JSON/XML parse helpers, `data2html`, and `_strip_newlines` through deterministic WordPress hook, URL, and escaping stubs. It does not claim live network/discovery, remote fetch transport, REST oEmbed controller behavior, installed routing/rendering, or generated public PHP replacement. |
| `WPHX-312.30` WP_Embed cache/autoembed detail | closed | Adds a PHP CLI copied-oracle/candidate fixture with 5 deterministic cases and 15 covered symbols across `class-wp-embed.php`, constructor hook/shortcode registration, `run_shortcode`, `shortcode` post-meta cache hits and remote-success cache writes, unknown fallback/link suppression, `autoembed`, `cache_oembed`, `delete_oembed_caches`, `find_oembed_post_id`, deterministic `wp_oembed_get`, post/meta storage, object cache, and `WP_Query` stubs. It does not claim live oEmbed network/discovery, installed editor/admin Ajax, REST oEmbed controller behavior, browser rendering, database-backed post/meta storage, or generated public PHP replacement. |
| `WPHX-312.31` oEmbed REST controller detail | closed | Adds a PHP CLI copied-oracle/candidate fixture with 9 deterministic cases and 15 covered symbols across `class-wp-oembed-controller.php`, REST route registration, filtered route maxwidth defaults, `get_item` success/error behavior, proxy permission checks, transient cache hits, local response short-circuit, remote oEmbed data fetch and `rest_oembed_ttl` cache writes, classic embed handler fallback with enqueued scripts, and proxy invalid-url errors through deterministic REST request, transient, oEmbed, `WP_Embed`, and script registry stubs. It does not claim full REST server dispatch, live oEmbed network/discovery, installed browser rendering, editor/admin flows, database-backed transients, or generated public PHP replacement. |
| `WPHX-312.32` HTTP proxy detail | closed | Adds a PHP CLI copied-oracle/candidate fixture with 7 deterministic cases and 17 covered symbols across `class-wp-http-proxy.php`, `WP_HTTP_Proxy` enablement/authentication accessors, authentication header encoding, malformed URL pass-through, localhost/site URL bypass, exact and wildcard `WP_PROXY_BYPASS_HOSTS` matching, and `pre_http_send_through_proxy` overrides through deterministic `get_option`/`apply_filters` stubs. It does not claim live HTTP transport routing, Requests/cURL/fsockopen integration, TLS/proxy negotiation, network I/O, installed distribution behavior, or generated public PHP replacement. |
| `WPHX-312.33` HTTP Requests bridge detail | closed | Adds a PHP CLI copied-oracle/candidate fixture with 5 deterministic cases and 20 covered symbols across `class-wp-http-requests-response.php`, `class-wp-http-requests-hooks.php`, real copied Requests support classes, `WP_HTTP_Requests_Response` object wrapping, case-insensitive header conversion, repeated header preservation, header/status/body mutation, cookie conversion into `WP_Http_Cookie`, `to_array` filename/status-message shape, parent Requests hook dispatch, `requests-*` WordPress action forwarding, and `curl.before_send`/`http_api_curl` compatibility. It does not claim live HTTP transport routing, Requests network I/O, redirect validation, proxy/TLS negotiation, installed distribution behavior, or generated public PHP replacement. |
| `WPHX-312.34` HTTP IXR client detail | closed | Adds a PHP CLI copied-oracle/candidate fixture with 7 deterministic cases and 13 covered symbols across `class-wp-http-ixr-client.php`, copied IXR support classes, URL constructor parsing for query strings/protocol-relative/relative/empty/explicit server inputs, XML-RPC request body construction through `IXR_Request`, custom and filtered headers, timeout inclusion/omission, `wp_safe_remote_post` handoff, `WP_Error` transport failure mapping, non-200 status failure mapping, malformed XML parse failure, XML-RPC fault mapping, and successful `methodResponse` parsing. It does not claim live HTTP transport, XML-RPC server behavior, pingback/comment integration, Requests/cURL/fsockopen behavior, installed distribution behavior, or generated public PHP replacement. |
| `WPHX-312.35` Feed cache transient detail | closed | Adds a PHP CLI copied-oracle/candidate fixture with 5 deterministic cases and 12 covered symbols across `class-wp-feed-cache-transient.php`, SimplePie Cache Base support, constructor transient/mod-name derivation, `wp_feed_cache_transient_lifetime` filtering, `save`/`load`/`mtime`/`touch`/`unlink` behavior over deterministic site transient stubs, array data persistence, SimplePie object data extraction, expiration handoff, and paired transient deletion. It does not claim live SimplePie feed parsing, remote feed fetching, object-cache backend behavior, multisite cache persistence, installed distribution behavior, or generated public PHP replacement. |
| `WPHX-312.36` SimplePie file HTTP detail | closed | Adds a PHP CLI copied-oracle/candidate fixture with 6 deterministic cases and 11 covered symbols across `class-wp-simplepie-file.php`, SimplePie parent/Misc support, Requests `CaseInsensitiveDictionary` support, remote HTTP constructor argument handoff, default user-agent omission, custom header and user-agent inclusion, successful response body/status/header capture, repeated-header normalization including content-type last-value behavior, `WP_Error` failure mapping, and non-HTTP URL failure behavior. It does not claim live network I/O, SimplePie parser behavior, feed XML parsing, Requests transport/TLS/proxy behavior, installed distribution behavior, or generated public PHP replacement. |
| `WPHX-312.37` SimplePie feed wrapper detail | closed | Adds a PHP CLI copied-oracle/candidate fixture with 4 deterministic cases and 14 covered symbols across `class-simplepie.php`, `class-feed.php`, copied feed adapter classes, deterministic SimplePie vendor/autoloader stubs, class-simplepie early return without native autoloader loading, compile-visible `wp_simplepie_autoload` declaration, native autoloader require handoff, deprecated autoload callback behavior, class-feed deprecation metadata, conditional class-simplepie loading, skipped class-simplepie loading when `SimplePie\SimplePie` already exists, and required feed adapter class availability. It does not claim SimplePie vendor parser behavior, live feed fetching, KSES sanitizer behavior, transient cache behavior, installed distribution behavior, or generated public PHP replacement. |
| `WPHX-312.38` HTTP response object detail | closed | Adds a PHP CLI copied-oracle/candidate fixture with 5 deterministic cases and 10 covered symbols across `class-wp-http-response.php`, constructor data/status/header initialization, `absint` status coercion, `get_data`/`set_data`, `get_headers`/`set_headers`, single-header replacement, single-header comma append when `replace=false`, direct `jsonSerialize` handoff to current data, and `json_encode` public-property serialization because the class does not implement `JsonSerializable`. It does not claim REST server dispatch, Requests transport integration, live HTTP behavior, installed distribution behavior, or generated public PHP replacement. |
| `WPHX-312.39` HTTP cookie object detail | closed | Adds a PHP CLI copied-oracle/candidate fixture with 6 deterministic cases and 11 covered symbols across `class-wp-http-cookie.php`, header-string parsing, array construction and requested-URL defaults, expires parsing, dynamic header attributes, domain/path/port matching in `test`, expired and nameless cookie rejection, `getHeaderValue`/`getFullHeader` behavior through `wp_http_cookie_value` filtering, and `get_attributes` shape. It does not claim live HTTP transport, Requests cookie jar behavior, browser cookie policy, installed distribution behavior, or generated public PHP replacement. |
| `WPHX-312.40` HTTP encoding detail | closed | Adds a PHP CLI copied-oracle/candidate fixture with 6 deterministic cases and 14 covered symbols across `class-wp-http-encoding.php`, `compress`/`decompress` roundtrip, gzuncompress and gzdecode fallback paths, `compatible_gzinflate` gzip-header and zlib-header behavior, invalid data passthrough, `accept_encoding` enabled and disabled branches for `decompress=false`, `stream=true`, and `limit_response_size`, `wp_http_accept_encoding` filter payload, `content_encoding`, `should_decode` array/string/empty behavior, and `is_available`. It does not claim live HTTP transport, server content negotiation, streaming transport behavior, installed distribution behavior, or generated public PHP replacement. |
| `WPHX-312.41` WP_Http helper detail | closed | Adds a PHP CLI copied-oracle/candidate fixture with 6 deterministic cases and 17 covered symbols across `class-wp-http.php`, `class-wp-http-cookie.php` support, `normalize_cookies`, `browser_redirect_compatibility`, `validate_redirects`, `make_absolute_url`, `handle_redirects`, `is_ip_address`, Requests cookie/response/exception stubs, redirect URL validation, absolute URL normalization, too-many-redirects errors, last `Location` selection, POST-to-GET redirect conversion, cookie filtering, and `wp_remote_request` handoff. It does not claim live HTTP transport, Requests network I/O, redirect safety beyond helper validation, proxy/TLS behavior, installed distribution behavior, or generated public PHP replacement. |
| `WPHX-312.42` WP_Http parser/header detail | closed | Adds a PHP CLI copied-oracle/candidate fixture with 5 deterministic cases and 12 covered symbols across `class-wp-http.php`, `class-wp-http-cookie.php` support, `processResponse`, `processHeaders`, `buildCookieHeader`, `chunkTransferDecode`, the deprecated protected `parse_url` wrapper, folded header unfolding, final redirected-response header selection, duplicate header arrays, `Set-Cookie` conversion to `WP_Http_Cookie`, scalar cookie upgrade, `wp_http_cookie_value` filtering, valid single-chunk decoding, multi-chunk CRLF passthrough, malformed chunk passthrough, and deprecation metadata. It does not claim live HTTP transport, Requests network I/O, redirect following, proxy/TLS behavior, installed distribution behavior, or generated public PHP replacement. |
| `WPHX-312.43` HTTP API wrapper/safety detail | closed | Adds a PHP CLI copied-oracle/candidate fixture with 5 deterministic cases and 26 covered symbols across `http.php`, `_wp_http_get_object`, safe and unsafe `wp_remote_*` wrappers, `wp_http_supports`, HTTP origin helpers, `wp_http_validate_url`, `allowed_http_request_hosts`, `ms_allowed_http_request_hosts`, deterministic `WP_Http` handoff, Requests capability checks, safe-wrapper `reject_unsafe_urls`, numeric capability normalization, HTTPS/SSL capability injection, origin filters, allowed-origin filters, protocol/userinfo/host rejection, same-home-port behavior, local-IP external-host filtering, safe-port filtering, redirect-host bridging, and multisite domain lookup caching. It does not claim live HTTP transport, Requests network I/O, DNS behavior beyond deterministic numeric/stubbed paths, CORS header emission, proxy/TLS behavior, installed distribution behavior, or generated public PHP replacement. |
| `WPHX-312.44` HTTP parse-url/class-http shim detail | closed | Adds a PHP CLI copied-oracle/candidate fixture with 5 deterministic cases and 8 covered symbols across `http.php`, `class-http.php`, `class-wp-http.php` support, `wp_parse_url`, `_get_component_from_parsed_url_array`, `_wp_translate_php_url_constant_to_key`, absolute URL component extraction, schemeless URL placeholder normalization, root-relative URL placeholder normalization with query-colon preservation, parse-failure `false`, missing component `null`, invalid component `null`/`false`, and the deprecated `class-http.php` handoff to `class-wp-http.php`. It does not claim live HTTP transport, Requests network I/O, URL safety validation, CORS header emission, installed distribution behavior, or generated public PHP replacement. |
| `WPHX-312.45` HTTP transport callback/test detail | closed | Adds a PHP CLI copied-oracle/candidate fixture with 5 deterministic cases and 18 covered symbols across `class-wp-http-streams.php`, `class-wp-http-curl.php`, `WP_Http_Streams::test`, `WP_Http_Curl::test`, `WP_HTTP_Fsockopen` inheritance, `use_streams_transport`/`use_curl_transport` filters, environment-aware streams/OpenSSL/cURL capability gates, and private `WP_Http_Curl::stream_headers`/`stream_body` callback state through reflection including header append, body append, `max_body_length` truncation, byte totals, and stream-handle writes. It does not claim live HTTP transport, socket/cURL execution, DNS/TLS/proxy behavior, certificate verification, redirect handling, streaming download over network, installed distribution behavior, or generated public PHP replacement. |
| `WPHX-312.46` WP_Http request orchestration detail | closed | Adds a PHP CLI copied-oracle/candidate fixture with 6 deterministic cases and 23 covered symbols across `class-wp-http.php`, `WP_Http::request`, `WP_Http::block_request`, `WP_HTTP_Requests_Hooks`, `WP_HTTP_Requests_Response`, `WP_HTTP_Proxy`, `WP_Http_Cookie`, default/filter argument construction, HEAD redirection override, `pre_http_request` short-circuiting, invalid and blocked request errors/debug actions, streaming filename and writability checks, header-string parsing, Requests option handoff for hooks, redirects, cookies, SSL verification, `data_format`, `max_bytes`, exception conversion, nonblocking response shape, and `http_response` filtering. It does not claim live Requests network I/O, real DNS/proxy/TLS behavior, transport execution, redirect following, installed distribution behavior, or generated public PHP replacement. |
| `WPHX-312.47` WP_Http block-request policy detail | closed | Adds a PHP CLI copied-oracle/candidate fixture with 5 deterministic cases and 10 covered symbols across `class-wp-http.php`, `WP_Http::block_request`, `WP_HTTP_BLOCK_EXTERNAL`, `WP_ACCESSIBLE_HOSTS`, disabled external blocking, malformed URL blocking, localhost and site-host bypasses, `block_local_requests` filter override, exact host allowlists, wildcard host allowlists, and default external blocking. It does not claim live HTTP transport, Requests network I/O, DNS resolution, proxy/TLS behavior, installed distribution behavior, or generated public PHP replacement. |
| `WPHX-312.48` WP_Http deprecated transport dispatch detail | closed | Adds a PHP CLI copied-oracle/candidate fixture with 6 deterministic cases and 11 covered symbols across `class-wp-http.php`, `WP_Http::_get_first_available_transport`, `WP_Http::_dispatch_request`, `http_api_transports`, `apply_filters_deprecated`, `http_api_debug`, `http_response`, fake `WP_Http_Curl`/`WP_Http_Streams` transport tests and requests, default curl-first selection, streams fallback, deprecated filter ordering, no-transport failure, dispatch success, response filtering, cached transport construction, and WP_Error preservation. It does not claim live HTTP transport, socket/cURL execution, Requests network I/O, DNS/TLS/proxy behavior, installed distribution behavior, or generated public PHP replacement. |
| `WPHX-312.49` WP_Http request proxy/safety option detail | closed | Adds a PHP CLI copied-oracle/candidate fixture with 6 deterministic cases and 18 covered symbols across `class-wp-http.php`, `class-wp-http-proxy.php`, `WP_Http::request`, `WP_HTTP_Proxy::send_through_proxy`, `WP_HTTP_Requests_Hooks`, `WP_HTTP_Requests_Response`, `WpOrg\Requests\Proxy\Http`, `http_request_reject_unsafe_urls`, `pre_http_send_through_proxy`, `requests.before_redirect`, `WP_Http::browser_redirect_compatibility`, `WP_Http::validate_redirects`, `wp_http_validate_url`, `wp_kses_bad_protocol`, proxy constants, safe URL validation, unsafe URL rejection before Requests execution, disallowed protocol stripping, proxy address/auth handoff, bypass hosts, site-host bypass, and proxy pre-filter overrides. It does not claim live Requests network I/O, real DNS/proxy/TLS negotiation, transport execution, installed distribution behavior, or generated public PHP replacement. |
| `WPHX-312.50` WP_Http_Encoding Haxe candidate detail | closed | Adds `src/wphx/wp/http/HttpEncodingStrategy.hx` plus a WPHX PHP compiler-emitted original-path `wp-includes/class-wp-http-encoding.php` public class adapter for `WP_Http_Encoding`, promoting 6 methods across `compress`, `decompress`, `compatible_gzinflate`, `content_encoding`, `should_decode`, and `is_available` to Haxe-owned behavior while preserving the public PHP class ABI, `#[AllowDynamicProperties]`, and native `wp_http_accept_encoding` filter boundary. The differential harness reuses the 6 WPHX-312.40 encoding cases for zlib roundtrip, fallback order, gzip/zlib header compatibility, accept-encoding enabled/disabled branches, and header decode checks. It does not claim live HTTP transfer encoding negotiation, streaming transport behavior, upstream HTTP PHPUnit parity, or installed distribution behavior beyond this bounded compiler-emitted class adapter. |
| `WPHX-312.51` WP_HTTP_Proxy Haxe candidate detail | closed | Adds `src/wphx/wp/http/HttpProxyStrategy.hx` plus a WPHX PHP compiler-emitted original-path `wp-includes/class-wp-http-proxy.php` public class adapter for `WP_HTTP_Proxy`, promoting the post-filter `send_through_proxy` routing decision to Haxe-owned behavior while preserving public PHP accessors, authentication header behavior, `parse_url`, `get_option('siteurl')`, proxy constants, and `pre_http_send_through_proxy` filter boundaries in PHP. The differential harness reuses the 7 WPHX-312.32 proxy cases for disabled defaults, authentication accessors, malformed URL pass-through, localhost/site bypass, exact bypass hosts, wildcard bypass hosts, and filter overrides. It does not claim live HTTP transport routing, Requests/cURL/fsockopen integration, TLS/proxy negotiation, upstream HTTP PHPUnit parity, installed distribution behavior, `WP_Http::request` ownership, or broader proxy ownership beyond this bounded compiler-emitted class adapter. |
| `WPHX-312.52` WP_HTTP_Response Haxe candidate detail | closed | Adds `src/wphx/wp/http/HttpResponseState.hx` plus a WPHX PHP compiler-emitted original-path `wp-includes/class-wp-http-response.php` public class adapter for `WP_HTTP_Response`, promoting constructor-equivalent state initialization, data/status/header accessors and mutators, header replacement/appending, and `jsonSerialize` data handoff to Haxe-owned behavior while preserving the public PHP class ABI, public properties, `#[AllowDynamicProperties]`, native `absint` boundary, and public-property `json_encode` behavior. The differential harness reuses the 5 WPHX-312.38 response-object cases for constructor initialization, mutators, replace/append headers, and JSON/public-property serialization. It does not claim REST server dispatch, Requests transport integration, live HTTP behavior, upstream HTTP PHPUnit parity, or installed distribution behavior beyond this bounded compiler-emitted class adapter. |
| `WPHX-312.53` WP_Http_Cookie Haxe candidate detail | closed | Adds module-level Haxe functions in `src/wphx/wp/http/HttpCookieStrategy.hx` plus a WPHX PHP compiler-emitted original-path `wp-includes/class-wp-http-cookie.php` public class adapter for `WP_Http_Cookie`, promoting post-construction `test` domain/path/port/expiry matching, `getHeaderValue` formatting around the native `wp_http_cookie_value` filter boundary, `getFullHeader`, and `get_attributes` shape to Haxe-owned behavior while preserving constructor parsing/defaults, public properties, `#[AllowDynamicProperties]`, dynamic Set-Cookie attributes, `parse_url`, `strtotime`, `urldecode`, `time`, and `apply_filters` boundaries. The differential harness reuses the 6 WPHX-312.39 cookie-object cases for header-string construction coverage, array defaults, matcher accept/reject cases, expired/nameless rejection, filtered headers, and attributes. It does not claim full constructor parsing ownership, Requests cookie jar behavior, live HTTP transport, browser cookie policy, upstream HTTP PHPUnit parity, or installed distribution behavior beyond this bounded compiler-emitted class adapter. |
| `WPHX-312.54` WP_Http block-request Haxe candidate detail | closed | Adds module-level Haxe functions in `src/wphx/wp/http/HttpBlockRequestPolicy.hx` plus a WPHX PHP compiler-emitted grouped original-path `wp-includes/class-wp-http.php` instance adapter for `WP_Http::block_request`, promoting localhost/site-host detection and `WP_ACCESSIBLE_HOSTS` exact/wildcard allowlist matching to Haxe-owned behavior while preserving `WP_HTTP_BLOCK_EXTERNAL`, `WP_ACCESSIBLE_HOSTS`, `parse_url`, `get_option('siteurl')`, and `block_local_requests` filter boundaries in PHP. The differential harness reuses the 5 WPHX-312.47 block-request cases for disabled blocking, malformed/external blocking, localhost/site-host bypass, local filter overrides, exact allowlists, and wildcard allowlists. It does not claim whole-file `WP_Http`, `WP_Http::request`, live HTTP transport, Requests network I/O, DNS/proxy/TLS behavior, or installed distribution behavior beyond this bounded compiler-emitted method adapter. |
| `WPHX-312.55` WP_Http IP address Haxe candidate detail | closed | Adds module-level Haxe functions in `src/wphx/wp/http/HttpIpAddress.hx` plus a WPHX PHP compiler-emitted grouped original-path `wp-includes/class-wp-http.php` adapter for `WP_Http::is_ip_address`, promoting IPv4 regex-shape detection, IPv6 detection, and bracket/space trim behavior to Haxe-owned behavior while preserving upstream PHP PCRE matching as a narrow WPHX-211 boundary. The differential harness reuses the WPHX-312.41 `wp-http:is-ip-address` case for IPv4, IPv6, bracketed IPv6, hostname false, and regex-shaped invalid IPv4 returning `4`. It does not claim broader `WP_Http` helper ownership, whole-file `WP_Http`, `WP_Http::request`, live HTTP transport, Requests network I/O, DNS/proxy/TLS behavior, or installed distribution behavior beyond this bounded compiler-emitted method adapter. |
| `WPHX-312.56` WP_Http browser redirect compatibility Haxe candidate detail | closed | Adds module-level Haxe functions in `src/wphx/wp/http/HttpRedirectCompatibility.hx` plus a WPHX PHP compiler-emitted grouped original-path `wp-includes/class-wp-http.php` adapter for `WP_Http::browser_redirect_compatibility`, promoting the 302 browser-compatible POST-to-GET decision to Haxe-owned behavior while preserving the public static method ABI, by-reference `$options` mutation, Requests response status property, and native `WpOrg\Requests\Requests::GET` boundary in PHP. The differential harness reuses the WPHX-312.41 `wp-http:browser-redirect-compatibility` case for 302 switching and 301 preservation. It does not claim broader redirect validation, absolute URL resolution, redirect following, whole-file `WP_Http`, `WP_Http::request`, live HTTP transport, Requests network I/O, or installed distribution behavior beyond this bounded compiler-emitted method adapter. |
| `WPHX-312.57` WP_Http redirect validation Haxe candidate detail | closed | Adds module-level Haxe functions in `src/wphx/wp/http/HttpRedirectValidation.hx` plus a WPHX PHP compiler-emitted grouped original-path `wp-includes/class-wp-http.php` adapter for `WP_Http::validate_redirects`, promoting the validation-failure branch decision to Haxe-owned behavior while preserving `wp_http_validate_url`, translation, `WpOrg\Requests\Exception`, and public static method ABI boundaries in PHP. The differential harness reuses the WPHX-312.41 `wp-http:validate-redirects` case for safe URL no-throw behavior and invalid URL exception class/message/type. It does not claim absolute URL resolution, redirect following, whole-file `WP_Http`, `WP_Http::request`, live HTTP transport, Requests network I/O, or installed distribution behavior beyond this bounded compiler-emitted method adapter. |
| `WPHX-312.58` WP_Http absolute URL Haxe candidate detail | closed | Adds module-level Haxe functions in `src/wphx/wp/http/HttpAbsoluteUrl.hx` plus a WPHX PHP compiler-emitted grouped original-path `wp-includes/class-wp-http.php` adapter for `WP_Http::make_absolute_url`, promoting URL assembly, schemeless host/port handling, root-relative path handling, relative parent path normalization, and query/fragment append behavior to Haxe-owned behavior while preserving `wp_parse_url`, parse-failure passthrough, PHP component emptiness checks, and the public static method ABI in PHP. The differential harness reuses the WPHX-312.41 `wp-http:make-absolute-url` case for absolute, schemeless, root-relative, relative-parent, query/fragment, empty-base, and parse-failure URL resolution. It does not claim redirect following, cookie filtering, whole-file `WP_Http`, `WP_Http::request`, live HTTP transport, Requests network I/O, or installed distribution behavior beyond this bounded compiler-emitted method adapter. |
| `WPHX-312.59` WP_Http redirect orchestration Haxe candidate detail | closed | Adds module-level Haxe functions in `src/wphx/wp/http/HttpRedirectOrchestration.hx` plus a WPHX PHP compiler-emitted grouped original-path `wp-includes/class-wp-http.php` adapter for `WP_Http::handle_redirects`, promoting redirect short-circuit checks, too-many-redirects detection, and POST 302/303 to GET method switching to Haxe-owned behavior while preserving native Location header array selection, local redirect budget mutation, `WP_Error` construction, `self::make_absolute_url`, cookie filtering, `wp_remote_request`, and the public static method ABI in PHP. The differential harness reuses the WPHX-312.41 `wp-http:handle-redirects` case for guard false returns, too-many redirects, last Location selection, absolute URL handoff, POST-to-GET conversion, redirect budget decrement, matching-cookie filtering, and recorded dispatch. It does not claim live redirect following, broader transport behavior, Requests network I/O, whole-file `WP_Http`, full `WP_Http::request`, or installed distribution behavior beyond this bounded compiler-emitted method adapter. |
| `WPHX-312.60` WP_Http processResponse Haxe candidate detail | closed | Adds module-level Haxe functions in `src/wphx/wp/http/HttpProcessResponse.hx`, reuses `src/wphx/wp/http/HttpChunkTransferDecode.hx`, and adds `src/wphx/wp/http/HttpDeprecatedParseUrl.hx` through a WPHX PHP compiler-emitted grouped original-path `wp-includes/class-wp-http.php` adapter for `WP_Http::processResponse`, `WP_Http::chunkTransferDecode`, and protected `WP_Http::parse_url`. It promotes the first CRLFCRLF header/body split, missing-body default, chunk-header detection, chunk-extension handling, single-chunk body extraction, malformed/non-chunk passthrough behavior, deprecated parse_url metadata, and `wp_parse_url` delegation to Haxe-owned behavior while preserving the public/protected static method ABIs, native PHP associative processResponse return shape, string chunkTransferDecode return shape, and subclass `parent::parse_url` access in PHP. The differential harness reuses the WPHX-312.42 `wp-http-parser:process-response`, `wp-http-parser:chunk-transfer-decode`, and `wp-http-parser:parse-url-wrapper` cases. It does not claim `processHeaders`, `buildCookieHeader`, live HTTP transport, installed distribution behavior, or whole-file `WP_Http` ownership beyond these bounded compiler-emitted method adapters. |
| `WPHX-312.61` WP_Http chunkTransferDecode Haxe candidate detail | closed | Adds module-level Haxe functions in `src/wphx/wp/http/HttpChunkTransferDecode.hx` plus a WPHX PHP compiler-emitted original-path `wp-includes/class-wp-http.php` adapter for `WP_Http::chunkTransferDecode`, promoting chunk-header detection, chunk-extension handling, single-chunk body extraction, and malformed/non-chunk passthrough behavior to Haxe-owned behavior while preserving the public static method ABI and string return shape in PHP. The differential harness reuses the WPHX-312.42 `wp-http-parser:chunk-transfer-decode` case for valid chunk decoding, extension decoding, non-chunk passthrough, malformed passthrough, and the upstream inter-chunk CRLF multi-chunk passthrough quirk. It does not claim `processHeaders`, `buildCookieHeader`, protected `parse_url`, live transfer-encoding negotiation, installed distribution behavior, or whole-file `WP_Http` ownership beyond this bounded compiler-emitted method adapter. |
| `WPHX-312.62` WP_Http processHeaders line-decision Haxe candidate detail | closed | Adds module-level Haxe functions in `src/wphx/wp/http/HttpProcessHeaders.hx` plus a WPHX PHP compiler-emitted grouped original-path `wp-includes/class-wp-http.php` adapter for `WP_Http::processHeaders`, promoting final response status-line detection, response code/message extraction, header key normalization, and header value trimming to Haxe-owned behavior while preserving PHP-native header arrays, folded-line preprocessing, final response slicing, duplicate header accumulation, `Set-Cookie` conversion to `WP_Http_Cookie`, public static method ABI, and native return shape in PHP. The differential harness reuses the WPHX-312.42 `wp-http-parser:process-headers` case for final response selection, folded-header unfolding, duplicate header arrays, discarded redirect headers, and converted cookies. It does not claim whole-file `WP_Http`, `WP_Http::request`, live HTTP transport, installed distribution behavior, or generated `WP_Http_Cookie` ownership beyond this bounded compiler-emitted method adapter. |
| `WPHX-312.63` WP_Http buildCookieHeader assembly Haxe candidate detail | closed | Adds a module-level Haxe function in `src/wphx/wp/http/HttpCookieHeaderAssembly.hx` plus a WPHX PHP compiler-emitted grouped original-path `wp-includes/class-wp-http.php` adapter for `WP_Http::buildCookieHeader( &$r )`, promoting append order, `; ` separator assembly, and trailing-delimiter avoidance for normalized cookie header values to Haxe-owned behavior while preserving PHP-native by-reference request array mutation, scalar-to-`WP_Http_Cookie` upgrading, `WP_Http_Cookie::getHeaderValue`, `wp_http_cookie_value` filter timing and payloads, public static method ABI, and native headers/cookies shapes in PHP. The differential harness reuses the WPHX-312.42 `wp-http-parser:build-cookie-header` case for scalar upgrade, object preservation, filtered values, filter payloads, and final `Cookie` header order. It does not claim whole-file `WP_Http`, `WP_Http::request`, live HTTP transport, installed distribution behavior, or generated `WP_Http_Cookie` ownership beyond this bounded compiler-emitted method adapter. |
| `WPHX-312.64` WP_Http deprecated parse_url wrapper Haxe candidate detail | closed | Adds module-level Haxe functions in `src/wphx/wp/http/HttpDeprecatedParseUrl.hx` plus a WPHX PHP compiler-emitted grouped original-path `wp-includes/class-wp-http.php` adapter for protected `WP_Http::parse_url`, promoting deprecation metadata (`WP_Http::parse_url`, `4.4.0`, `wp_parse_url()`) and the wrapper handoff to `wp_parse_url` to Haxe-owned behavior while preserving the protected static PHP method ABI, subclass `parent::parse_url` access pattern, native `_deprecated_function` dispatch, and native `wp_parse_url` false-or-array result shape in PHP. The differential harness reuses the WPHX-312.42 `wp-http-parser:parse-url-wrapper` case for parsed URL component shape and deprecation metadata. It does not claim `wp_parse_url` internals, whole-file `WP_Http`, `WP_Http::request`, live HTTP transport, or installed distribution behavior beyond this bounded compiler-emitted method adapter. |
| `WPHX-312.65` WP_Http request nonblocking response Haxe candidate detail | closed | Adds a module-level Haxe function in `src/wphx/wp/http/HttpRequestNonblocking.hx` plus a WPHX PHP compiler-emitted original-path `wp-includes/class-wp-http.php` bounded adapter for `WP_Http::request( $url, $args = [] )`, promoting only the native nonblocking response array shape returned after successful Requests dispatch when `blocking=false` to Haxe-owned behavior while preserving public `WP_Http::request` ABI, defaults/filter/preempt/error/blocking/streaming/header/cookie/SSL/proxy/hook setup, Requests dispatch, exception conversion, `http_api_debug` timing, `is_wp_error` handling, and native PHP false/null leaves in PHP. The differential harness reuses the WPHX-312.46 `wp-http-request:exception-and-nonblocking` case for Requests exception conversion plus nonblocking option handoff and response shape. It does not claim full request orchestration, live Requests network I/O, transport execution, redirect following, installed distribution behavior, or whole-file `WP_Http` ownership beyond this bounded compiler-emitted adapter. |
| `WPHX-312.66` WP_Http deprecated transport selection Haxe candidate detail | closed | Adds module-level Haxe functions in `src/wphx/wp/http/HttpTransportSelection.hx` plus a WPHX PHP compiler-emitted original-path `wp-includes/class-wp-http.php` bounded `WP_Http` adapter for public `_get_first_available_transport` and private `_dispatch_request`, promoting the default deprecated transport token list, core token recognition, and `WP_Http_*` class-name construction to Haxe-owned behavior while preserving `apply_filters_deprecated('http_api_transports')` timing/payload, static transport `test` calls, private `_dispatch_request` transport instance caching, `WP_Error` construction, `http_api_debug` action timing, `http_response` filtering, and fake transport request behavior in PHP. The differential harness reuses all 6 WPHX-312.48 transport dispatch cases for default cURL selection, streams fallback, deprecated order filtering, no-transport false, dispatch success, and dispatch error preservation. It does not claim live cURL/streams execution, Requests network I/O, DNS/TLS/proxy behavior, `WP_Http::request`, full dispatch ownership, upstream HTTP PHPUnit parity, or installed distribution behavior beyond this bounded compiler-emitted class adapter. |
| `WPHX-312.67` WP_Http request safety options Haxe candidate detail | closed | Adds a module-level Haxe function in `src/wphx/wp/http/HttpRequestSafetyOptions.hx` plus a generated-PHP candidate shell for `WP_Http::request`, promoting only the redirect-validation hook registration condition (`wp_kses_bad_protocol` availability plus enabled `reject_unsafe_urls`) to Haxe-owned behavior while preserving public `WP_Http::request` ABI, `http_request_reject_unsafe_urls` filter timing/payload, `wp_http_validate_url` and `wp_kses_bad_protocol` execution, hook object mechanics, browser redirect hook registration, proxy option/auth handoff, `pre_http_send_through_proxy` overrides, Requests dispatch, debug/error handling, and native PHP arrays/objects in PHP. The differential harness reuses all 6 WPHX-312.49 request proxy/safety cases for safe URL validation hooks, unsafe URL rejection, bad protocol stripping, proxy authentication handoff, proxy bypass hosts, and pre-filter overrides. It does not claim full request safety ownership, URL validation internals, bad-protocol stripping internals, live Requests network I/O, proxy negotiation, transport execution, installed distribution behavior, or durable generated original-path PHP replacement. |
| `WPHX-312.68` WP_Http request method options Haxe candidate detail | closed | Adds a module-level Haxe function in `src/wphx/wp/http/HttpRequestMethodOptions.hx` plus a WPHX compiler-emitted original-path `wp-includes/class-wp-http.php` request shell, promoting only the non-GET/HEAD `data_format='body'` Requests option decision to Haxe-owned behavior while preserving public `WP_Http::request` ABI, method defaults and filters, HEAD redirection override, hooks, header parsing, cookie normalization, SSL verification, `max_bytes` handoff, Requests dispatch, response filtering, and native PHP arrays/objects in PHP. The differential harness reuses the WPHX-312.46 `wp-http-request:head-defaults` and `wp-http-request:headers-cookies-ssl-post` cases for HEAD no-body-format behavior and PUT body-format handoff. It does not claim full request orchestration, method normalization, body serialization, live Requests network I/O, transport execution, redirect following, installed distribution behavior, or whole-file `WP_Http` ownership. |
| `WPHX-312.69` WP_Http request redirect options Haxe candidate detail | closed | Adds a module-level Haxe function in `src/wphx/wp/http/HttpRequestRedirectOptions.hx` plus a WPHX compiler-emitted original-path `wp-includes/class-wp-http.php` request shell, promoting only the branch decision that a zero parsed redirection count disables Requests `follow_redirects` while nonzero counts keep the `redirects` option path to Haxe-owned behavior. It preserves public `WP_Http::request` ABI, redirection defaults and filters, HEAD redirection override, hook registration, header/cookie/SSL/`max_bytes`/`data_format` handoff, Requests dispatch, response filtering, and native PHP arrays/objects in PHP. The differential harness reuses the WPHX-312.46 `wp-http-request:head-defaults` and `wp-http-request:headers-cookies-ssl-post` cases for HEAD `follow_redirects=false` and PUT `redirects=2` handoff. It does not claim redirect following, redirect count filtering semantics, `handle_redirects` behavior, live Requests network I/O, transport execution, installed distribution behavior, or whole-file `WP_Http` ownership. |
| `WPHX-312.70` WP_Http request SSL options Haxe candidate detail | closed | Adds a module-level Haxe function in `src/wphx/wp/http/HttpRequestSslOptions.hx` plus a WPHX compiler-emitted original-path `wp-includes/class-wp-http.php` request shell, promoting only the pre-filter branch decision that `sslverify=false` sets Requests `verify=false` and `verifyname=false`, while `sslverify=true` keeps the certificate-path verify branch. It preserves public `WP_Http::request` ABI, defaults and filters, `sslcertificates` value handoff, `https_ssl_verify` filter timing and payload, hook registration, headers/cookies/redirects/`max_bytes`/`data_format` handoff, Requests dispatch, response filtering, and native PHP arrays/objects in PHP. The differential harness reuses the WPHX-312.46 `wp-http-request:head-defaults` and `wp-http-request:headers-cookies-ssl-post` cases for default certificate-path verification and disabled SSL verification handoff. It does not claim TLS or certificate validation behavior, `https_ssl_verify` filter semantics, live Requests network I/O, transport execution, installed distribution behavior, full `WP_Http::request`, or whole-file `WP_Http` ownership. |
| `WPHX-312.71` WP_Http request response-size options Haxe candidate detail | closed | Adds a module-level Haxe function in `src/wphx/wp/http/HttpRequestResponseSizeOptions.hx` plus a WPHX compiler-emitted original-path `wp-includes/class-wp-http.php` request shell, promoting only the branch decision that a non-null `limit_response_size` sets Requests `max_bytes` while the default null value omits `max_bytes`. It preserves public `WP_Http::request` ABI, defaults and filters, hook registration, headers/cookies/redirects/SSL/`data_format` handoff, streaming filename behavior, Requests dispatch, response filtering, and native PHP arrays/objects in PHP. The differential harness reuses the WPHX-312.46 `wp-http-request:head-defaults` and `wp-http-request:headers-cookies-ssl-post` cases for default no-limit behavior and explicit `limit_response_size=12` handoff. It does not claim actual response truncation behavior, Requests transfer handling, live network I/O, transport execution, installed distribution behavior, full `WP_Http::request`, or whole-file `WP_Http` ownership. |
| `WPHX-312.72` WP_Http request stream blocking Haxe candidate detail | closed | Adds a module-level Haxe function in `src/wphx/wp/http/HttpRequestStreamBlocking.hx` plus a generated-PHP candidate shell for `WP_Http::request`, promoting only the decision that `stream=true` forces the parsed `blocking` option to `true` before Requests dispatch. It preserves public `WP_Http::request` ABI, defaults and filters, stream filename defaulting, writable-directory validation, hook registration, headers/cookies/redirects/SSL/`max_bytes`/`data_format` handoff, Requests dispatch, response filtering, and native PHP arrays/objects in PHP. The differential harness reuses the WPHX-312.46 `wp-http-request:streaming` case for default filename handoff, forced blocking, and bad-destination error preservation. It does not claim streaming file I/O behavior, temp-directory or basename semantics, writable-directory policy, Requests transfer handling, live network I/O, transport execution, installed distribution behavior, or durable generated original-path PHP replacement. |
| `WPHX-312.73` WP_Http request stream filename options Haxe candidate detail | closed | Adds a module-level Haxe function in `src/wphx/wp/http/HttpRequestStreamFilenameOptions.hx` plus a WPHX compiler-emitted original-path `wp-includes/class-wp-http.php` request shell, promoting only the decision that `stream=true` sets the Requests `filename` option from the already-resolved parsed filename. It preserves public `WP_Http::request` ABI, defaults and filters, stream filename defaulting, writable-directory validation, forced blocking behavior, hook registration, headers/cookies/redirects/SSL/`max_bytes`/`data_format` handoff, Requests dispatch, response filtering, and native PHP arrays/objects in PHP. The differential harness reuses the WPHX-312.46 `wp-http-request:streaming` case for default filename handoff, forced blocking, and bad-destination error preservation. It does not claim streaming file I/O behavior, temp-directory or basename semantics, writable-directory policy, filename path normalization, Requests transfer handling, live network I/O, transport execution, installed distribution behavior, full `WP_Http::request`, or whole-file `WP_Http` ownership. |
| `WPHX-312.74` WP_Http request invalid URL Haxe candidate detail | closed | Adds a module-level Haxe function in `src/wphx/wp/http/HttpRequestInvalidUrl.hx` plus a WPHX compiler-emitted original-path `wp-includes/class-wp-http.php` request shell, promoting only the post-parse decision that an empty request URL or missing parsed URL scheme takes the invalid-url `WP_Error` branch. It preserves public `WP_Http::request` ABI, `pre_http_request` behavior, unsafe URL filtering, `parse_url` execution, `WP_Error` construction, translation, `http_api_debug` action payload and timing, `block_request` behavior, Requests dispatch avoidance, and native PHP arrays/objects in PHP. The differential harness reuses the WPHX-312.46 `wp-http-request:invalid-and-blocked` case for relative URL rejection, blocked absolute URL preservation, debug-action count, and no Requests dispatch. It does not claim URL parsing, URL validation or sanitization semantics, `WP_Error` internals, block-request policy, live network I/O, transport execution, installed distribution behavior, full `WP_Http::request`, or whole-file `WP_Http` ownership. |
| `WPHX-312.75` WP_Http request blocked-request Haxe candidate detail | closed | Adds a module-level Haxe function in `src/wphx/wp/http/HttpRequestBlockedRequest.hx` plus a WPHX compiler-emitted original-path `wp-includes/class-wp-http.php` request shell, promoting only the decision that a true `WP_Http::block_request` result takes the blocked-request `WP_Error` branch. It preserves public `WP_Http::request` ABI, URL filtering/parsing, invalid-url handling, `WP_Http::block_request` execution and policy, `WP_Error` construction, translation/`sprintf`, `http_api_debug` action payload and timing, Requests dispatch avoidance, and native PHP arrays/objects in PHP. The differential harness reuses the WPHX-312.46 `wp-http-request:invalid-and-blocked` case for blocked absolute URL rejection, relative invalid URL preservation, debug-action count, and no Requests dispatch. It does not claim `block_request` internals, host allowlist policy, URL parsing or validation semantics, `WP_Error` internals, live network I/O, transport execution, installed distribution behavior, full `WP_Http::request`, or whole-file `WP_Http` ownership. |
| `WPHX-312.76` WP_Http request preemptive response Haxe candidate detail | closed | Adds a module-level Haxe function in `src/wphx/wp/http/HttpRequestPreemptiveResponse.hx` plus a WPHX compiler-emitted original-path `wp-includes/class-wp-http.php` request shell, promoting only the decision that a `pre_http_request` value other than boolean false short-circuits and returns that native value. It preserves public `WP_Http::request` ABI, `pre_http_request` filter timing/payload, native returned array/WP_Error value, URL filtering/parsing, invalid/blocked request branches, Requests dispatch avoidance, and native PHP arrays/objects in PHP. The differential harness reuses the WPHX-312.46 `wp-http-request:preempt` case for preempted response body/code and Requests-not-called behavior. It does not claim filter semantics, returned response shape validation, `WP_Error` internals, live network I/O, transport execution, installed distribution behavior, full `WP_Http::request`, or whole-file `WP_Http` ownership. |
| `WPHX-312.77` WP_Http request header parsing Haxe candidate detail | closed | Adds a module-level Haxe function in `src/wphx/wp/http/HttpRequestHeaderParsing.hx` plus a generated-PHP candidate shell for `WP_Http::request`, promoting only the decision that headers which are not already a PHP array route through `self::processHeaders` before Requests dispatch. It preserves public `WP_Http::request` ABI, null-to-empty-array normalization, native PHP header arrays, `processHeaders` semantics and Set-Cookie handling, request defaults/filters, hook registration, cookie/SSL/redirect/body/`max_bytes` handoff, Requests dispatch, response filtering, and native PHP arrays/objects in PHP. The differential harness reuses the WPHX-312.46 `wp-http-request:head-defaults` and `wp-http-request:headers-cookies-ssl-post` cases for default array-header bypass and string-header parsing. It does not claim null header normalization, header parsing semantics, Set-Cookie conversion, live network I/O, transport execution, installed distribution behavior, or durable generated original-path PHP replacement. |
| `WPHX-312.78` WP_Http request proxy options Haxe candidate detail | closed | Adds a module-level Haxe function in `src/wphx/wp/http/HttpRequestProxyOptions.hx` plus a generated-PHP candidate shell for `WP_Http::request`, promoting only the decision that a Requests proxy option is attached when `WP_HTTP_Proxy` is enabled and `send_through_proxy` returns true. It preserves public `WP_Http::request` ABI, `WP_HTTP_Proxy` policy and filters, proxy constants, host/bypass matching, authentication handoff, native `WpOrg\Requests\Proxy\Http` object construction, Requests dispatch, response filtering, and native PHP arrays/objects in PHP. The differential harness reuses the WPHX-312.49 proxy cases for authenticated proxy handoff, site/wildcard bypasses, external proxy use, and `pre_http_send_through_proxy` false/true overrides. It does not claim proxy policy internals, authentication property semantics, live proxy negotiation, live network I/O, transport execution, installed distribution behavior, or durable generated original-path PHP replacement. |
| `WPHX-312.79` WP_Http request proxy authentication Haxe candidate detail | closed | Adds a module-level Haxe function in `src/wphx/wp/http/HttpRequestProxyAuthentication.hx` plus a generated-PHP candidate shell for `WP_Http::request`, promoting only the decision that `WP_HTTP_Proxy::use_authentication()` true enables authentication properties and credential handoff on the already-created Requests proxy object. It preserves public `WP_Http::request` ABI, proxy enable/send-through policy, proxy constants, host/bypass matching, username/password values, native `WpOrg\Requests\Proxy\Http` object construction, Requests dispatch, response filtering, and native PHP arrays/objects in PHP. The differential harness reuses the WPHX-312.49 proxy cases for authenticated proxy handoff and proxy handoff without authentication. It does not claim proxy policy internals, username/password value semantics, live proxy negotiation, live network I/O, transport execution, installed distribution behavior, or durable generated original-path PHP replacement. |
| `WPHX-312.80` WP_Http request error response Haxe candidate detail | closed | Adds a module-level Haxe function in `src/wphx/wp/http/HttpRequestErrorResponse.hx` plus a generated-PHP candidate shell for `WP_Http::request`, promoting only the decision that a response identified as `WP_Error` after Requests dispatch and `http_api_debug` returns immediately before nonblocking and `http_response` filter handling. It preserves public `WP_Http::request` ABI, Requests dispatch, exception conversion, `WP_Error` construction/internals, `is_wp_error` evaluation, `http_api_debug` timing/payload, nonblocking response shape, response filtering, and native PHP arrays/objects in PHP. The differential harness reuses the WPHX-312.46 `wp-http-request:exception-and-nonblocking` case for exception conversion, WP_Error early return, and non-error continuation to nonblocking handling. It does not claim exception conversion, `WP_Error` internals, `is_wp_error` semantics, nonblocking response shape, live network I/O, transport execution, installed distribution behavior, or durable generated original-path PHP replacement. |
| `WPHX-312.81` WP_Http request cookie options Haxe candidate detail | closed | Adds a module-level Haxe function in `src/wphx/wp/http/HttpRequestCookieOptions.hx` plus a generated-PHP candidate shell for `WP_Http::request`, promoting only the decision that non-empty parsed request cookies are normalized and attached to Requests options before dispatch. It preserves public `WP_Http::request` ABI, request defaults/filters, native parsed args arrays, `self::normalize_cookies` behavior, `WP_Http_Cookie`/request cookie semantics, Requests cookie option shape, header/SSL/redirect/body/`max_bytes` handoff, Requests dispatch, response filtering, and native PHP arrays/objects. The differential harness reuses WPHX-312.46 `wp-http-request:head-defaults` and `wp-http-request:headers-cookies-ssl-post` for empty-cookie omission and non-empty cookie normalization handoff. It does not claim cookie normalization internals, `WP_Http_Cookie` internals, Requests cookie jar behavior, live network I/O, transport execution, installed distribution behavior, or durable generated original-path PHP replacement. |
| `WPHX-312.82` WP_Http request stream destination error Haxe candidate detail | closed | Adds a module-level Haxe function in `src/wphx/wp/http/HttpRequestStreamDestinationError.hx` plus a generated-PHP candidate shell for `WP_Http::request`, promoting only the decision that a PHP-owned stream destination writability check failure returns the streaming destination `WP_Error` before Requests dispatch. It preserves public `WP_Http::request` ABI, request defaults/filters, stream filename defaulting, `dirname`/`wp_is_writable` evaluation, `WP_Error` construction, translation, `http_api_debug` timing/payload, forced blocking behavior, header/cookie/redirect/SSL/`max_bytes`/`data_format` handoff, Requests dispatch, response filtering, and native PHP arrays/objects. The differential harness reuses WPHX-312.46 `wp-http-request:streaming` for valid stream continuation and unwritable destination error behavior. It does not claim filename resolution, writability semantics, `WP_Error` internals, streaming file I/O, live network I/O, transport execution, installed distribution behavior, or durable generated original-path PHP replacement. |
| `WPHX-312.83` WP_Http request HEAD redirection default Haxe candidate detail | closed | Adds a module-level Haxe function in `src/wphx/wp/http/HttpRequestHeadRedirectionDefault.hx` plus a WPHX compiler-emitted original-path `wp-includes/class-wp-http.php` request shell, promoting only the pre-parse decision that an explicitly supplied `HEAD` method sets the default redirection count to `0` before `wp_parse_args` merges defaults. It preserves public `WP_Http::request` ABI, `wp_parse_args` behavior, default/filter values, method normalization, parsed args, Requests redirect option handoff, generated `processHeaders()` and `normalize_cookies()` neighboring adapters, Requests dispatch, response filtering, and native PHP arrays/objects. The differential harness reuses WPHX-312.46 `wp-http-request:head-defaults` and `wp-http-request:headers-cookies-ssl-post` for HEAD redirect suppression and non-HEAD redirect handoff. It does not claim argument parsing, filter semantics, method normalization, cookie normalization ownership beyond this generated public adapter, redirect following, live network I/O, transport execution, installed distribution behavior, or whole-file `WP_Http` ownership. |
| `WPHX-312.84` WP_Http request unsafe URL validation Haxe candidate detail | closed | Adds a module-level Haxe function in `src/wphx/wp/http/HttpRequestUnsafeUrlValidation.hx` plus a generated-PHP candidate shell for `WP_Http::request`, promoting only the decision that `reject_unsafe_urls` runs `wp_http_validate_url()` while the `wp_kses_bad_protocol` boundary exists. It preserves public `WP_Http::request` ABI, `http_request_reject_unsafe_urls` filtering, `function_exists` behavior, `wp_http_validate_url` semantics, URL mutation shape, bad-protocol stripping, invalid-url `WP_Error` construction/debug timing, hook registration, proxy handoff, Requests dispatch, response filtering, and native PHP arrays/objects. The differential harness reuses WPHX-312.49 request proxy/safety cases for safe validation, unsafe rejection, bad-protocol stripping, proxy handoff, bypass, and pre-filter overrides. It does not claim URL validation internals, bad-protocol stripping internals, filter semantics, live network I/O, proxy negotiation, transport execution, installed distribution behavior, or durable generated original-path PHP replacement. |
| `WPHX-312.85` WP_Http request bad-protocol stripping Haxe candidate detail | closed | Adds a module-level Haxe function in `src/wphx/wp/http/HttpRequestBadProtocolStripping.hx` plus a generated-PHP candidate shell for `WP_Http::request`, promoting only the decision that a non-empty current URL value runs `wp_kses_bad_protocol()` while the `wp_kses_bad_protocol` boundary exists. It preserves public `WP_Http::request` ABI, `http_request_reject_unsafe_urls` filtering, `function_exists` behavior, `wp_http_validate_url` semantics, URL mutation shape, bad-protocol stripping semantics, invalid-url `WP_Error` construction/debug timing, hook registration, proxy handoff, Requests dispatch, response filtering, and native PHP arrays/objects. The differential harness reuses WPHX-312.49 request proxy/safety cases for safe validation, unsafe rejection, bad-protocol stripping, proxy handoff, bypass, and pre-filter overrides. It does not claim URL validation internals, `wp_kses_bad_protocol` internals, filter semantics, live network I/O, proxy negotiation, transport execution, installed distribution behavior, or durable generated original-path PHP replacement. |
| `WPHX-312.86` WP_Http request redirection-copy Haxe candidate detail | closed | Adds a module-level Haxe function in `src/wphx/wp/http/HttpRequestRedirectionCopy.hx` plus a generated-PHP candidate shell for `WP_Http::request`, promoting only the decision that a missing parsed `_redirection` value copies the current `redirection` count while a pre-supplied `_redirection` value is preserved. It preserves public `WP_Http::request` ABI, defaults and filters, native parsed-args array mutation, redirect count values, hook construction, Requests redirect option handoff, Requests dispatch, response filtering, and native PHP arrays/objects. The differential harness observes the protected `WP_HTTP_Requests_Hooks` request snapshot for missing-copy and pre-supplied-preserve cases. It does not claim redirect following, redirect count filtering semantics, `handle_redirects` behavior, live Requests network I/O, transport execution, installed distribution behavior, or durable generated original-path PHP replacement. |
| `WPHX-312.87` WP_Http request null-header normalization Haxe candidate detail | closed | Adds a module-level Haxe function in `src/wphx/wp/http/HttpRequestNullHeaderNormalization.hx` plus a generated-PHP candidate shell for `WP_Http::request`, promoting only the decision that parsed request `headers` equal to `null` normalize to an empty native PHP array before hook construction and Requests dispatch. It preserves public `WP_Http::request` ABI, parsed args, native parsed-args array mutation, header values, `processHeaders` semantics, hook construction, Requests option handoff, Requests dispatch, response filtering, and native PHP arrays/objects. The differential harness observes both Requests headers and the protected `WP_HTTP_Requests_Hooks` request snapshot for null-normalize and array-preserve cases. It does not claim header parsing semantics, Set-Cookie conversion, live Requests network I/O, transport execution, installed distribution behavior, or durable generated original-path PHP replacement. |
| `WPHX-312.88` WP_Http request stream default-filename Haxe candidate detail | closed | Adds a module-level Haxe function in `src/wphx/wp/http/HttpRequestStreamDefaultFilename.hx` plus a generated-PHP candidate shell for `WP_Http::request`, promoting only the decision that streaming requests with an empty parsed `filename` assign the PHP-owned `get_temp_dir() . basename( $url )` default filename before writable-directory checks and Requests dispatch. It preserves public `WP_Http::request` ABI, request defaults and filters, temp-dir lookup, basename/path semantics, native parsed-args array mutation, writable-directory validation, forced blocking behavior, Requests filename option handoff, Requests dispatch, response filtering, streaming file I/O, and native PHP arrays/objects. The differential harness observes default filename assignment, explicit filename preservation, forced blocking, and bad-destination error behavior. It does not claim temp-dir or basename semantics, writable-directory policy, streaming file I/O, live Requests network I/O, transport execution, installed distribution behavior, or durable generated original-path PHP replacement. |
| `WPHX-312.89` WP_Http request http_response attachment Haxe candidate detail | closed | Adds a module-level Haxe function in `src/wphx/wp/http/HttpRequestHttpResponseAttachment.hx` plus a generated-PHP candidate shell for `WP_Http::request`, promoting only the decision that successful Requests responses attach the original `WP_HTTP_Requests_Response` wrapper to the public response array under `http_response` before debug and response-filter handling. It preserves public `WP_Http::request` ABI, request defaults and filters, Requests dispatch, response wrapper construction, response array conversion, debug/filter timing, error and nonblocking exits, response filtering, and native PHP arrays/objects. The differential harness observes the successful HEAD request response array, the attached summarized `WP_HTTP_Requests_Response`, and continued `http_response` filter application. It does not claim wrapper construction, `to_array()` conversion, filter semantics, live Requests network I/O, transport execution, installed distribution behavior, or durable generated original-path PHP replacement. |
| `WPHX-312.90` WP_Http request mbstring reset Haxe candidate detail | closed | Adds a module-level Haxe function in `src/wphx/wp/http/HttpRequestMbstringReset.hx` plus a generated-PHP candidate shell for `WP_Http::request`, promoting only the decision that `reset_mbstring_encoding()` runs after the Requests dispatch/catch boundary before debug, error, nonblocking, and response-filter handling. It preserves public `WP_Http::request` ABI, mbstring encoding semantics, Requests dispatch, exception conversion, response conversion, debug/filter timing, error and nonblocking exits, response filtering, and native PHP arrays/objects. The differential harness observes reset calls after both a thrown Requests exception and a successful nonblocking Requests dispatch while preserving exception conversion and nonblocking response shape. It does not claim mbstring internals, `mbstring_binary_safe_encoding()` semantics, transport execution, installed distribution behavior, or durable generated original-path PHP replacement. |
| `WPHX-313` Media/images/filesystem/uploads | closed | Domain closure receipt exists across the bounded surface inventory, first typed Haxe media upload adapter-contract candidate, upload validation oracle fixture, image metadata/editor oracle fixture, filesystem credentials oracle fixture, image/filesystem adapter-contract candidate, selected upstream PHPUnit ratchet group scope, and packaged installed-style media/upload gate. Closure is bounded: public PHP media/filesystem/upload files are still copied oracle source or bridge routers, generated public PHP replacement is not claimed, complete upstream media PHPUnit pass/pass parity is not claimed, real GD/Imagick/AVIF/HEIC/PDF/EXIF/IPTC execution is not claimed, remote FTP/SSH filesystem transport parity is not claimed, database-backed installed media persistence is deferred, updater/admin orchestration is deferred, and this does not close the whole WordPress media subsystem beyond the recorded evidence. |
| `WPHX-313.01` Media/filesystem/upload surface detail | closed | Adds a bounded surface inventory for 37 PHP runtime source files, 37 PHP distribution artifacts, 666 ABI entries, 22 classes, and 81 related upstream tests across media library helpers, attachment thumbnails, image metadata/editing, upload validation and endpoints, MIME/filetype security, and filesystem/upload IO. The manifest records cross-domain handoffs for attachments-as-posts (`WPHX-307`), theme/customizer media controls (`WPHX-310`), REST attachment upload routes (`WPHX-311`), media blocks (`WPHX-314`), admin media screens/AJAX/list tables (`WPHX-315`/`WPHX-316`), multisite quotas/paths (`WPHX-317`), and updater filesystem credential flows (`WPHX-319`). It does not claim public PHP replacement, Haxe-owned runtime logic, live filesystem or image-library parity, REST/admin upload parity, or installed-distribution media parity. |
| `WPHX-313.02` Media upload adapter-contract detail | closed | Adds a documented module-level Haxe contract in `src/wphx/wp/media/MediaUploadAdapterContract.hx` plus a generated-PHP candidate probe with 31 deterministic observations across upload validation, MIME/filetype routing, unique filename routing, attachment metadata/image-editor handoff, filesystem backend/credential routing, and media hook intent. The runner compiles through the stock Haxe PHP target, lints generated PHP, audits the Haxe source for escape hatches, and records manifest, ownership, and receipt evidence. It does not claim public PHP replacement, native file IO, image editor execution, REST/admin upload flows, multisite quota behavior, installed media behavior, or durable original-path PHP adapter ownership. |
| `WPHX-313.03` Media upload validation oracle detail | closed | Adds a copied-oracle/candidate PHP fixture with 10 deterministic cases and 7 covered symbols across `wp_unique_filename`, `wp_check_filetype`, `wp_check_filetype_and_ext`, `_wp_handle_upload`, `wp_handle_sideload`, `wp_upload_dir`, and `get_allowed_mime_types`. It observes filename collision incrementing, uppercase extension lowercasing, allowed text MIME validation, missing-file extension fallback, executable MIME rejection, invalid form submission, PHP upload error conversion, empty sideload rejection, MIME rejection, and successful sideload return shape through a deterministic `pre_move_uploaded_file` filter. The success case records `photo-1.txt`, upload/MIME/unique/pre-move/final hooks, and normalized file/URL/type output. This remains bridge evidence from copied upstream PHP and does not claim Haxe-owned runtime logic, public PHP replacement, image editor execution, REST/admin upload flows, multisite quota behavior, installed media behavior, or durable original-path PHP adapter ownership. |
| `WPHX-313.04` Image metadata/editor oracle detail | closed | Adds a copied-oracle/candidate PHP fixture with 14 deterministic cases and 12 covered symbols across `wp_get_image_editor`, `_wp_image_editor_choose`, `wp_image_editor_supports`, `wp_get_image_editor_output_format`, `image_resize_dimensions`, `image_make_intermediate_size`, `wp_get_registered_image_subsizes`, `wp_get_missing_image_subsizes`, `wp_update_image_subsizes`, `wp_generate_attachment_metadata`, `wp_read_image_metadata`, and `file_is_displayable_image`. The fixture injects a fake image editor only through the public `wp_image_editors` filter so WordPress still owns editor selection, method checks, `WP_Error` propagation, return shaping, registered-size decisions, missing-subsize comparison, invalid-attachment handling, filesize-only non-image metadata, and displayable-image failure behavior. This remains bridge evidence from copied upstream PHP and does not claim Haxe-owned runtime logic, public PHP replacement, GD/Imagick/AVIF/HEIC execution, real EXIF/IPTC extraction, PDF previews, real subsize generation, REST/admin upload flows, installed media behavior, or durable original-path PHP adapter ownership. |
| `WPHX-313.05` Filesystem credentials oracle detail | closed | Adds a copied-oracle/candidate PHP fixture with 9 deterministic cases and 12 covered symbols across `get_filesystem_method`, `request_filesystem_credentials`, `WP_Filesystem`, `WP_Filesystem_Base`, `WP_Filesystem_Direct`, and direct `connect`/`put_contents`/`get_contents`/`copy`/`move`/`delete`/`dirlist` behavior. It observes direct writable-context method selection, `filesystem_method` filter override payloads, credentials filter short-circuiting, direct credential success, valid submitted credential normalization and non-secret option storage, invalid-nonce form output and secret clearing, direct filesystem bootstrap/permission constants, missing transport file `null` behavior, and local direct filesystem I/O on temporary files. This remains bridge evidence from copied upstream PHP and does not claim Haxe-owned runtime logic, public PHP replacement, FTP extension/sockets/SSH2 transport execution, remote path mapping, updater/install orchestration, admin modal parity, installed media behavior, or durable original-path PHP adapter ownership. |
| `WPHX-313.06` Image/filesystem adapter-contract detail | closed | Adds a documented module-level Haxe contract in `src/wphx/wp/media/ImageFilesystemAdapterContract.hx` plus a generated-PHP candidate probe with 40 deterministic observations across image editor selection, intermediate size return shaping, missing subsize and metadata update decisions, filesystem credential/method routing, and direct local filesystem I/O intent. The runner compiles through the stock Haxe PHP target, lints generated PHP, audits the Haxe source for escape hatches, and records manifest, ownership, and receipt evidence. It does not claim public PHP replacement, native image-library execution, EXIF/IPTC extraction, FTP/SSH transports, updater/admin orchestration, installed media behavior, or durable original-path PHP adapter ownership. |
| `WPHX-313.07` Media/filesystem upstream PHPUnit ratchet detail | closed | Declares 4 selected upstream PHPUnit ratchet groups covering 33 media, image editor/metadata, upload/MIME/filesystem, REST attachment, and AJAX media test files. The refreshed ratchet run is deterministic but blocked on missing local PHPUnit runtime inputs (`vanilla_phpunit_vendor_missing`, `vanilla_wp_tests_config_missing`, and `candidate_root_env_missing`), so no upstream PHPUnit pass/pass parity is claimed. This scope does not claim public PHP replacement, complete upstream media suite parity, native image-library parity, remote filesystem transport parity, installed admin/media behavior, or durable original-path PHP adapter ownership. |
| `WPHX-313.08` Media/upload installed-style gate detail | closed | Adds a packaged oracle/candidate installed-style HTTP gate with 7 deterministic cases across media/upload package boundary, upload validation and unique filename intent, attachment response shape, image metadata/editor/subsize intent, REST attachment upload intent, AJAX image edit intent, and filesystem direct credential/direct I/O intent. It mirrors selected locked upstream media/upload/filesystem PHP files into oracle and candidate package roots, serves matching observations through PHP's built-in server, and records manifest, ownership, and receipt evidence. This remains bridge evidence from copied upstream PHP plus deterministic routers: it does not claim generated public PHP replacement, real native image-library execution, EXIF/IPTC extraction, remote FTP/SSH transports, database-backed installed media persistence, full REST/admin media behavior, updater orchestration, or durable original-path PHP adapter ownership. |
| `WPHX-313.09` Media/filesystem/upload domain closure detail | closed | Closes the current bounded evidence slice across `WPHX-313.01` through `WPHX-313.08` with a domain-closure receipt. This is source-domain evidence closure only: it records what is covered and preserves remaining gates for provisioned upstream PHPUnit execution, database-backed installed media persistence, real native image-library/editor behavior, remote filesystem transports, updater/admin orchestration, and generated original-path public PHP adapters. |
| `WPHX-314` Blocks/block parser/render/supports/bindings/interactivity PHP | active | `WPHX-314.01` adds a bounded surface inventory for 140 PHP runtime source files, 140 PHP distribution artifacts, 1,137 ABI entries, 46 classes, and 163 related upstream tests across block parser state, block registration/rendering, core block library renderers, block supports, block bindings, block hooks/patterns, style engine, HTML API, fonts/assets, and interactivity. `WPHX-314.02` adds the first typed Haxe adapter-contract candidate, `WPHX-314.03` adds copied-oracle/candidate parser/render behavior evidence for 5 deterministic cases, `WPHX-314.04` adds copied-oracle/candidate supports/bindings evidence for 6 deterministic cases, `WPHX-314.05` adds copied-oracle/candidate block pattern registry evidence for 6 deterministic cases, `WPHX-314.06` adds copied-oracle/candidate block hooks insertion evidence for 6 deterministic cases, `WPHX-314.07` adds copied-oracle/candidate style engine evidence for 6 deterministic cases, `WPHX-314.08` adds copied-oracle/candidate HTML API tag processor evidence for 6 deterministic cases, `WPHX-314.09` adds copied-oracle/candidate interactivity API evidence for 6 deterministic cases, `WPHX-314.10` adds copied-oracle/candidate core block renderer evidence for 6 deterministic cases, `WPHX-314.11` declares 6 selected upstream PHPUnit ratchet groups covering 89 block/style/HTML/interactivity test files, and `WPHX-314.12` adds installed-style HTTP evidence for 7 deterministic block rendering cases; the current ratchet run is deterministic but blocked on missing local PHPUnit runtime inputs, so no upstream PHPUnit pass/pass parity is claimed. It records handoffs to posts/query/taxonomy/comments (`WPHX-307`/`WPHX-308`), themes/templates/theme.json/global styles (`WPHX-310`), REST block endpoints (`WPHX-311`), HTTP/feed/embed/media-backed blocks (`WPHX-312`/`WPHX-313`), admin/editor flows (`WPHX-315`/`WPHX-316`), and browser/Gutenberg package work (`WPHX-400`/`WPHX-500`). Public PHP replacement, Haxe-owned runtime logic, editor/browser ownership, full database-backed installed block parity, upstream PHPUnit pass/pass parity, and generated original-path adapter ownership are not claimed. |
| `WPHX-314.01` Blocks/interactivity surface detail | closed | Adds deterministic manifest, ownership, and receipt evidence for WPHX-314 source/distribution/ABI/test inventory, grouped by block assets, bindings, parser, patterns, registration/rendering, related registries, supports, core block library renderers, fonts API, HTML API, interactivity API, and style engine. It separates named and anonymous class ABI entries, records by-reference and conditional declaration risks, and names next fixture gates for parser roundtrip, render context, supports serialization, binding resolution, block hooks, patterns/styles, style engine, HTML API, interactivity hydration/directives, selected core block renderers, and selected upstream PHPUnit ratchets. |
| `WPHX-314.02` Blocks/interactivity adapter-contract detail | closed | Adds a documented module-level Haxe contract in `src/wphx/wp/blocks/BlocksInteractivityAdapterContract.hx` plus a generated-PHP candidate probe with 50 deterministic observations across block parser, render, supports, bindings, hooked-block insertion, patterns, style engine, HTML API, interactivity, fonts/assets, and hook intent. The runner compiles through the stock Haxe PHP target, lints generated PHP, audits the Haxe source for escape hatches, and records manifest, ownership, and receipt evidence. It does not claim public PHP replacement, editor/browser ownership, installed block parity, upstream PHPUnit pass/pass, or Gutenberg package ownership. |
| `WPHX-314.03` Block parser/render oracle detail | closed | Adds a copied-oracle/candidate PHP fixture with 5 deterministic cases and 21 covered symbols across `parse_blocks`, `serialize_block`, `serialize_blocks`, `filter_block_content`, `register_block_type`, `render_block`, `do_blocks`, `WP_Block_Parser`, `WP_Block_Type`, `WP_Block_Type_Registry`, `WP_Block_List`, and `WP_Block`. It observes freeform and serialized parsing, nested block roundtrip serialization, dynamic render callbacks with context, `pre_render_block` short-circuiting, and `do_blocks` rendering through mirrored WordPress 7.0 PHP. This remains bridge evidence from copied upstream PHP and does not claim Haxe-owned runtime logic, public PHP replacement, block supports/bindings/style/interactivity ownership, editor/browser ownership, installed block parity, upstream PHPUnit pass/pass, or durable original-path PHP adapter ownership. |
| `WPHX-314.04` Block supports/bindings oracle detail | closed | Adds a copied-oracle/candidate PHP fixture with 6 deterministic cases and 25 covered symbols across `WP_Block_Supports`, `get_block_wrapper_attributes`, generated/custom classname support, anchor support, aria-label support, `wp_should_skip_block_supports_serialization`, `block_has_support`, block binding source registration, supported-attribute filters, `WP_Block::process_block_bindings`, `WP_Block::render`, and `render_block`. It observes wrapper attribute merging, support attribute registration, aria-label skip serialization, static and filtered binding attributes, binding source callbacks with context, and registry validation/unregister behavior through mirrored WordPress 7.0 PHP. This remains bridge evidence from copied upstream PHP and does not claim Haxe-owned runtime logic, public PHP replacement, full style engine/global styles/layout/HTML API/interactivity ownership, editor/browser ownership, installed block parity, upstream PHPUnit pass/pass, or durable original-path PHP adapter ownership. |
| `WPHX-314.05` Block patterns registry oracle detail | closed | Adds a copied-oracle/candidate PHP fixture with 6 deterministic cases and 19 covered symbols across `register_block_pattern`, `unregister_block_pattern`, pattern category registration, `wp_normalize_remote_block_pattern`, `WP_Block_Patterns_Registry`, `WP_Block_Pattern_Categories_Registry`, and the `apply_block_hooks_to_content` boundary used during pattern retrieval. It observes content-backed registration, lazy `filePath` include/caching, outside-init tracking, invalid registration diagnostics, category get/all/unregister behavior, and remote `block_types`/`viewport_width` normalization through mirrored WordPress 7.0 PHP. This remains bridge evidence from copied upstream PHP and does not claim Haxe-owned runtime logic, public PHP replacement, real block hook insertion, remote REST/theme directory loading, full style/HTML/interactivity ownership, editor/browser ownership, installed block parity, upstream PHPUnit pass/pass, or durable original-path PHP adapter ownership. |
| `WPHX-314.06` Block hooks insertion oracle detail | closed | Adds a copied-oracle/candidate PHP fixture with 6 deterministic cases and 19 covered symbols across `get_hooked_blocks`, `insert_hooked_blocks`, `set_ignored_hooked_blocks_metadata`, `insert_hooked_blocks_and_set_ignored_hooked_blocks_metadata`, `apply_block_hooks_to_content`, `apply_block_hooks_to_content_from_post_object`, before/after visitors, traversal serialization, single-instance suppression, post-object wrapper handling, and ignored hooked block metadata. It observes registry mapping, filter-added and suppressed hooked blocks, ignored metadata skips, before/after/first-child/last-child insertion, `supports.multiple=false` suppression, and post meta/root ignored-hook reference behavior through mirrored WordPress 7.0 PHP. This remains bridge evidence from copied upstream PHP and does not claim Haxe-owned runtime logic, public PHP replacement, installed REST/template integration, full style/HTML/interactivity ownership, editor/browser ownership, installed block parity, upstream PHPUnit pass/pass, or durable original-path PHP adapter ownership. |
| `WPHX-314.07` Style engine oracle detail | closed | Adds a copied-oracle/candidate PHP fixture with 6 deterministic cases and 22 covered symbols across `wp_style_engine_get_styles`, `wp_style_engine_get_stylesheet_from_css_rules`, `wp_style_engine_get_stylesheet_from_context`, `WP_Style_Engine`, `WP_Style_Engine_CSS_Declarations`, `WP_Style_Engine_CSS_Rule`, `WP_Style_Engine_CSS_Rules_Store`, and `WP_Style_Engine_Processor`. It observes declaration filtering, compact and pretty CSS compilation, nested rule groups, processor optimization/selector combining, preset-derived classnames and CSS variables, context store replay, invalid selector skips, rule removal, and static store registry behavior through mirrored WordPress 7.0 PHP. This remains bridge evidence from copied upstream PHP plus deterministic helper stubs and does not claim Haxe-owned runtime logic, public PHP replacement, global styles/theme.json resolver integration, HTML API/interactivity ownership, editor/browser ownership, installed block parity, upstream PHPUnit pass/pass, or durable original-path PHP adapter ownership. |
| `WPHX-314.08` HTML API tag processor oracle detail | closed | Adds a copied-oracle/candidate PHP fixture with 6 deterministic cases and 33 covered symbols across `WP_HTML_Tag_Processor`, `WP_HTML_Decoder`, and `WP_Token_Map`. It observes tag query and traversal, decoded attributes, `data-*` prefix matching, class list and membership, attribute/class mutation, URI rejection, boolean attributes, bookmark seek/release behavior, token text and comment reads/mutations, special-element modifiable text escaping for script/style/title/textarea, incomplete-token pausing, self-closing flags, tag closer behavior, BR closer normalization, and namespace switching through mirrored WordPress 7.0 PHP. This remains bridge evidence from copied upstream PHP plus deterministic helper stubs and does not claim Haxe-owned runtime logic, public PHP replacement, full HTML5 processor/tree ownership, interactivity ownership, editor/browser ownership, installed block parity, upstream PHPUnit pass/pass, or durable original-path PHP adapter ownership. |
| `WPHX-314.09` Interactivity API oracle detail | closed | Adds a copied-oracle/candidate PHP fixture with 6 deterministic cases and 33 covered symbols across `wp_interactivity`, `wp_interactivity_process_directives`, `wp_interactivity_state`, `wp_interactivity_config`, `wp_interactivity_data_wp_context`, `wp_interactivity_get_context`, `wp_interactivity_get_element`, `WP_Interactivity_API`, `WP_Interactivity_API_Directives_Processor`, server directive processors, client-data filters, router-region handling, and data-wp-each expansion. It observes state/config merging, data-wp-context helper encoding, outside-processing diagnostics, server directive processing for bind/class/style/text/context/element access, derived state hydration tracking, each-template expansion, router side effects, SVG skip diagnostics, unbalanced fallback, client-navigation script attributes, router data filters, and balanced-template helper behavior through mirrored WordPress 7.0 PHP. This remains bridge evidence from copied upstream PHP plus deterministic helper stubs and does not claim Haxe-owned runtime logic, public PHP replacement, browser/client `@wordpress/interactivity` package ownership, installed block parity, upstream PHPUnit pass/pass, or durable original-path PHP adapter ownership. |
| `WPHX-314.10` Core block renderer oracle detail | closed | Adds a copied-oracle/candidate PHP fixture with 6 deterministic cases and 22 covered symbols across `blocks/categories.php`, `blocks/archives.php`, `blocks/tag-cloud.php`, `render_block_core_categories`, `build_dropdown_script_block_core_categories`, `render_block_core_archives`, `block_core_archives_build_dropdown_script`, `render_block_core_tag_cloud`, registration callbacks, deterministic taxonomy/category/archive/tag-cloud helpers, wrapper attributes, inline script tags, unique IDs, REST/admin predicates, and bounded HTML API link mutation. It observes category list enhanced-pagination click attributes, category dropdown selected query vars and front-end script injection, archive empty fallback output, weekly archive dropdown label/ID/script behavior, tag cloud font-size unit parsing/count output, REST empty-placeholder behavior, and metadata registration callback handoff through mirrored WordPress 7.0 PHP. This remains bridge evidence from copied upstream PHP plus deterministic helper stubs and does not claim Haxe-owned runtime logic, public PHP replacement, full core block renderer matrix coverage, installed block registry/theme/editor behavior, browser/client package ownership, upstream PHPUnit pass/pass, or durable original-path PHP adapter ownership. |
| `WPHX-314.11` Blocks/interactivity upstream PHPUnit ratchet detail | closed | Declares 6 selected upstream PHPUnit ratchet groups covering 89 block/style/HTML/interactivity test files: `blocks-parser-render-core`, `blocks-supports-bindings-core`, `blocks-hooks-patterns-core`, `style-engine-core`, `html-api-core`, and `interactivity-api-core`. The refreshed ratchet run is deterministic but blocked on missing local PHPUnit runtime inputs (`vanilla_phpunit_vendor_missing`, `vanilla_wp_tests_config_missing`, and `candidate_root_env_missing`), so no upstream PHPUnit pass/pass parity is claimed. This scope does not claim public PHP replacement, installed block registry/theme/editor behavior, browser/Gutenberg package ownership, complete upstream block suite parity, Haxe-owned runtime logic, or durable original-path PHP adapter ownership. |
| `WPHX-314.12` Installed-style block rendering detail | closed | Adds a packaged oracle/candidate installed-style HTTP gate with 7 deterministic cases across package boundary, front-end block rendering, block supports/wrapper attributes, block hooks/pattern insertion intent, style engine output, selected categories/archives/tag-cloud core renderers, and interactivity directive/state/config rendering. It mirrors selected locked upstream block, style-engine, HTML API, interactivity, and core renderer PHP files into oracle and candidate package roots and serves matching JSON observations through PHP's built-in server. This remains bridge evidence from copied upstream PHP plus deterministic installed-style routers: it does not claim Haxe-owned runtime logic, generated public PHP replacement, full database-backed installed block editor behavior, browser/Gutenberg package ownership, upstream PHPUnit pass/pass, complete core renderer matrix coverage, or durable original-path PHP adapter ownership. |
| `WPHX-317` Multisite/network | closed | Domain closure receipt exists across all 7 child tasks: surface inventory, site/network option plus site-transient fixtures, blog switch/cache fixtures, site/network ABI/query fixtures, bootstrap/domain-path routing fixtures, signup/lifecycle/counts/quota fixtures, and the first typed Haxe multisite adapter-contract candidate. Public PHP replacement and installed multisite distribution claims remain later gates. |
| `WPHX-322` PHP first-party manifest closure | closed | Manifest/split closure assigns 1,564 C1 PHP runtime source entries and 1,222 C1 PHP distribution artifacts to closed domains or explicit follow-up owners, with zero unassigned; PHP vendor entries are excluded to WPHX-323 |

## Gutenberg Plan

The plan remains: **WordPress PHP first, browser platform feasibility next, Gutenberg package port in a sibling repo only when justified.**

1. Keep `../gutenberg` as the forward Gutenberg 23.4 oracle checkout. Do not mix it into the WordPress 7.0 distribution track without an ADR.
2. Use this repo for `WPHX-400` feasibility: genes-ts output mode, package exports, WordPress script handles, React/TSX or HHX policy, source maps, bundling, globals, and browser tests.
3. Create `../gutenberghx` only after the first real package and React/TSX feasibility gates pass, currently described as after `WPHX-403` and `WPHX-404`, or earlier only by ADR.
4. When `gutenberghx` exists, keep this repo as the program authority: root Beads database, PRD, release claims, lock manifests, distribution integration, and final receipts.
5. Let `gutenberghx` own Haxe source for Gutenberg packages, package-level fixtures, generated package artifacts, and package parity receipts.
6. Port packages by dependency layer. Start with small leaf packages and package-shape fixtures, then move toward shared primitives and editor/block packages only after exports, React semantics, data stores, source maps, and build output are proven.
7. Smaller libraries stay in this repo unless they need independent release cadence, reusable compiler-pressure fixtures, or sustained parallel ownership. If split later, they still need lock manifests and receipts consumed by this repo.

## Update Rules

Update this file when any of the following changes:

- a `WPHX-*` parent milestone opens, closes, is seeded, or has meaningful child progress;
- a task changes the super-progress estimate or its weights;
- a receipt creates new evidence for Haxe ownership, parity, ABI, generated-code shape, distribution behavior, or nonfunctional closure;
- the Gutenberg/gutenberghx trigger, baseline, or cross-repo workflow changes;
- a shell/scaffold graduates to verified Haxe-owned logic, or a verified slice is downgraded because evidence was found weak;
- an ADR changes the PRD milestone shape, baseline policy, compiler strategy, or test authority.

Progress claims in chat or handoff notes should point back here, Beads, and receipts. Do not let this matrix become a second task database.
